# Coding agent (checkpoint one)

A terminal coding agent built on Helyx. It is the first product and it proves the substrate.

## Goal

From the TUI, prompt the agent about a repository. It reads, edits, and runs shell commands in a loop. Output streams. Abort, steer, and follow-up work during a turn. The session resumes after a restart. One user: the author, on this repository.

## Shape

- Root `helyx` Mix project. All bundled plugins live in one Mix project, `plugins/bundled` (app `helyx_plugins`), and the product lives in `apps/coding_agent`. Both depend on Helyx by path, and the product depends on `helyx_plugins` and on `ex_ratatui`, which is optional there (ADR 0005). No umbrella.
- Elixir 1.19 and OTP 28, pinned in `.tool-versions`. Started with a Mix task.

## Runtime

- One session process per conversation, started under a DynamicSupervisor and found by id through Registry.
- Each turn has a turn id and runs in a Task under Core's task supervisor. The session monitors the Task and ignores messages from a Task that is no longer current.
- One hands process per session runs tool calls as Tasks and spawns harness programs. See ADR 0003.
- Core starts as a child spec that takes the plugin list. No application config.
- Interface modes: Provider, Tool, and Event are `multi`. ModelContext, Compaction, and Transport are `single`.

### Abort

Abort ends the current turn by id. The session kills the turn Task and tells the hands to cancel that turn id. The hands kill every tool Task for that turn and stop every operating system process it started for that turn. Closing a port does not stop the command behind it, so the hands start each shell command and each harness program in its own process group, send `SIGTERM` to the group on abort, and send `SIGKILL` after a short grace period. The hands reply to the session only after every process in the group is gone, and the session starts the next turn only after that reply. Every tool result and harness event carries its turn id, and the session drops any that arrive for a turn that is no longer current. A shell command killed mid-write can leave a half-written file. That is accepted.

When a turn is aborted, by the user or by a restart, every tool call in it that has no result gets a tool result entry with `is_error` true and the text `aborted`. The entry is appended to the transcript, so the next provider call sees a complete call and result pair. Providers reject a tool call without a result, so removing the call is not an option.

### Steer and follow-up queues

Queued steers join the transcript as user messages, in order, before the next provider call inside the turn. Queued follow-ups start a new turn after the current turn ends normally; anything still queued at that point, steers included, becomes that one new turn's prompt, steers first, so no typed message is lost. A steer or follow-up sent with no turn running starts a turn at once, like a prompt, so the client never races the end of a turn. Abort and turn failure drop both queues. Every change emits a `queue_update` event; the drain at a normal turn end goes out between turns with a nil turn id, and `Helyx.Session.queue_count/1` reads the counts. Each queue holds at most 32 entries; past the cap the call returns `{:error, :queue_full}` and the TUI keeps the text in the composer and shows the reason in the status bar (#46).

## Interfaces and bundled plugins

| Interface | Plugins in this checkpoint |
|---|---|
| Provider | `OpenAI` (model provider, OpenAI wire format, first target OpenCode Go or Zen), `ClaudeCode` (harness, `claude -p` stream-json over stdio), `Codex` (harness, `codex app-server` JSON-RPC over stdio), `Fake` (scripted, for tests) |
| Tool | `Read`, `Bash`, `Edit`, `Write` |
| ModelContext | `Default`: base prompt plus `AGENTS.md` files from home down to the working directory |
| Compaction | `None`: no-op |
| Event | the TUI subscribes; no bundled handlers |
| Transport | `Local`: OTP messages in one node |

A model is named by one string, `provider/model`. A session parses it once per start, resume, and switch.

### Model switching

`Helyx.Session.set_model/2` switches the model of a running session. The ref goes through the same parse and the same provider lookup as the `:model` of `Session.start`. A bad ref returns `{:error, {:invalid_model_ref, ref}}`, an unknown prefix returns `{:error, {:unknown_provider, id}}`, a prefix that two plugins share returns `{:error, {:ambiguous_provider, id}}`, and in every case the model, the file, and the event stream stay as they were. An accepted switch does three things inside one session call: it appends a `model_change` entry to the session file, it replaces the session's model ref and provider module, and it emits a `model_change` event with the new ref. A switch to the current model is accepted and recorded like any other.

A turn fixes its model and provider when it starts. A switch during a turn is accepted at once, but every provider call of the running turn, and the `model` of its assistant messages, stay on the old model. The next turn, a queued follow-up too, uses the new one. The switch belongs to no turn, so its event carries a nil turn id, during a turn too.

A `model_change` append that fails on disk is treated like a message append that fails: a warning, persistence off for the session, and the switch still holds in memory.

Both directions between a model provider and a harness provider go through this one path, because the session sees only a provider module. The harness half is tested when the first harness provider exists (#10).

### Harness turns

A harness runs its own loop and its own tools. Helyx starts the program for the turn, translates its output into messages and events, and records them in the transcript. Helyx tools are not visible to the harness (ADR 0002).

- Steer: a harness does not accept a message inside a turn. On a harness turn, a steer aborts the turn and starts a new turn with the steer as its prompt. The transcript keeps whatever the harness completed before the abort.
- Resume: each harness issues its own session id. Helyx writes it to the transcript as a `harness_session` entry when the first harness turn starts, and passes it back on later turns and after a restart. If the harness no longer has that session, the next harness turn starts a fresh harness session, the transcript gets a new `harness_session` entry, and the TUI shows a notice that the harness lost its own context. The Helyx transcript is unaffected. Replaying the transcript into a fresh harness session is out of scope.

## Bounds

| What | Bound | Over the bound |
| ---- | ----- | -------------- |
| file read (read, edit, model context) | one regular file, at most 10,485,760 bytes, valid UTF-8 | a device, a directory, or a larger file is an error; invalid UTF-8 is an error, `binary file, N bytes` |
| tool result text | 2000 lines or 51,200 bytes of line content, on whole lines | read keeps the head and bash keeps the tail; the result says which absolute lines it shows, and a truncated head names the offset that continues the read, when lines follow the shown lines. When the line at the kept edge (the first of a head window, the last of a tail) is over the byte cap by itself, it is cut to the cap and is the only line shown; the notice names the line and the bytes the tool kept (`, line N cut at B bytes`), and no offset reaches the rest of that line. When the cut line of a head window is the last line of the text, the notice has no `read again` clause, because that offset returns nothing from `truncate/3` (#61), and it is an error from the read tool (#78). Only the bytes at the cut edge decide what the cut loses (#68): the cut loses the fewest bytes, 0 to 3, that leave a whole character at the edge, so the shown line is 51,197 to 51,200 bytes. That removes a part of a character (a cut inside one, or a prefix that no valid character has, such as `0xC0` or `0xED 0xA0`) and up to 3 invalid bytes that stand at the edge, such as a `0xFF` or continuation bytes with no lead byte. When no loss of at most 3 bytes leaves a whole character there (text that is all `0xFF`, four continuation bytes), the edge loses 3 bytes and stays invalid. Invalid bytes anywhere else in the line, such as a partial character at the far end of `head -c` output, do not change the cut. The hands replace every invalid byte that stays. The cut is an accepted ceiling (#50 triage), and opencode's read also marks a cut line (`docs/research/coding-tools.md`, opencode). Line numbers in a bash result count from the kept output buffer (next row). B counts the tool's own text: when the hands replace invalid bytes afterwards (row "tool result text validity"), the delivered line can be up to three times B. |
| read tool `offset` | a positive integer, a 1-based line number. A missing or `null` offset is line 1. A float with no fraction, such as `2001.0`, is its integer, because the JSON encoders of some models send it; this holds for a float of any size. The tool has no `limit` argument, and it ignores a `limit` key like any key that the schema does not name (#75). pi and opencode both have `offset` and `limit` (`docs/research/coding-tools.md`, pi and opencode). A `limit` argument needs a line count in `Helyx.Tool.truncate/3`, which #75 does not change. This is accepted: offset and the truncation notice reach every line, so the tool needs no `limit`. An offset after the last line is an error result, `offset N is after the last line: PATH has T lines` (#78, closed), so an offset that is too large does not look like an empty file. The ticket preferred the error. The tools research records no tool that reports an offset after the last line (`docs/research/coding-tools.md`), so the choice has no source to cite. Every such offset gives an empty window from `Helyx.Tool.truncate/3`. Thus only an empty window after line 1 pays for the count. The count is one pass over the bytes that builds no list and does not split the text again, so its memory does not grow with the file. It uses the line rule of `truncate/3`. One trailing newline ends the last line. More trailing newlines are blank lines that count. An offset at a trailing blank line is thus an ok result that can be empty; the last line that a truncation notice can name is such a line at most, never a line after the last one. The empty file is the one exception to the line rule: it has 0 lines here, and `truncate/3` counts 1. Thus offset 1 or no offset gives an empty ok result, and a larger offset is the error. The error shows an offset above 1,000,000,000 as `over 1000000000`, because no file within the 10 MiB read limit has that many lines, so the text does not grow with the digits of the argument. For the same reason `truncate/3` gets at most that offset, so the time of the read does not grow with the digits. PATH is the `path` argument as the model gave it, as in the older `cannot read PATH` error. Its only bound is the 10 MiB limit on tool call bytes | an error result before the file is read, `offset must be a positive integer (a 1-based line number), got K`, never a silent default. K names the kind of the value (`a string`, `an integer below 1`, `a number that is not a whole number of 1 or more`, `a boolean`, `an array`, `an object`) and never shows the value, so the size and the cost of the text do not depend on the argument; the longest text of this error is 111 bytes. This bound is on the tool only. The session encodes the arguments of every tool call to JSON for the session file, and that cost is quadratic in the digits of a large integer argument of any tool (measured 2.5 s for 400,000 digits, under the 10 MiB limit on tool call bytes). This is older than #75 and is open, ticket #79. The other tools (`bash`, `edit`, `write`) have only string arguments, and a wrong type there is already an error result from the catch-all clause of `run/2` |
| bash output buffer while the command runs | the last 204,800 bytes | older output is dropped, the result says so; a byte cut that lands inside a character drops the rest of that character, so the kept tail of valid output starts on a character boundary and a cut keeps 204,797 to 204,800 bytes (#51); output that was never valid also loses at most 3 leading continuation bytes and stays invalid for the next row |
| tool result text validity | valid UTF-8 before it enters the transcript | the hands replace each invalid byte sequence with one U+FFFD (`String.replace_invalid/1`) before the session sees the result; replacement runs after a tool's own truncation and can grow the text to at most three times its byte size |
| bash command and working directory | no NUL byte; port arguments are NUL-terminated C strings that would cut the string silently | an error result before the port opens; nothing runs |
| bash working directory at command start | must be a directory the watchdog can enter; the hands check it, but it can vanish or be empty afterwards | closed by structure (#52): the port has no cd option; the watchdog does the `chdir` itself before it forks, and a failure writes the marker with `0`, which is never a group id, then the OS reason. The command is held until the go-ahead, so no command output can come before the marker or pass for it. A `pipe` or `fork` that fails in the watchdog uses the same marker (it was exit status 91 in an ok result). The reason carries the directory as given and goes through the tool result truncation. The result is an error, `the command did not start: cannot enter the working directory ...`; nothing is forked, and a real exit status 2 stays an ok result |
| bash watchdog marker read | the marker line is `<nonce> <number>`, and it must end within the first 4,096 bytes of the merged stream; only a line that ends within that limit counts, so which marker is found, if any, does not depend on how the stream is cut into messages. The nonce is 8 random bytes for each call, and only the watchdog's arguments carry it. Whole lines before the marker can only be perl's own startup output (a bad locale warning prints environment values, which can hold any line but not the nonce); they are kept in front of the output | the only path to the go-ahead and to an ok result is a group marker, registered with the hands first. A stream that ends, or whose first 4,096 bytes hold no marker line, is an error, `the command did not start: the watchdog gave no marker: ...`; the tool closes the port, so the watchdog kills the child it holds (a child whose watchdog is already dead gets end of file on the go-ahead pipe and exits without the command). No window size decides whether an unregistered command runs (#52) |
| bash watchdog, perl variables of the environment | The input is every variable whose name starts with `PERL`, but not `PERL_BADLANG`. perlrun names `PERL_UNICODE`, `PERL5OPT`, `PERL5LIB`, `PERLLIB`, `PERL5DB`, `PERLIO`, and more, and each one can change the interpreter. `PERL_UNICODE` makes a `sysread` or a `syswrite` fatal. `PERL5OPT=-d` starts the debugger on the watchdog's stdin. `PERL5LIB` can replace the POSIX module. The rule is the prefix and not a list, so a variable of a later perl is also covered. A name that is not perl's costs nothing, because every value comes back. The bound on the values is the OS limit on arguments and environment together, as before. Each variable adds 12 bytes to that total, so an environment within those bytes of the limit now fails at the start of perl; not reproduced | closed by structure (#71): the port starts the watchdog without these variables (the `:env` option of `Port.open`), so no `PERL*` variable except `PERL_BADLANG` changes how the watchdog runs. Each value stays in the environment under the name `HELYX_KEEP_<name>`, which perl does not read, so only the same user can see it, as before. The watchdog gives each value its name back in `%ENV` before the fork, which does not change an interpreter that runs already. The command sees the user's value of each variable and no `HELYX_KEEP_PERL` name. The prefix `HELYX_KEEP_PERL` is reserved. The watchdog takes a variable of the user with that prefix for one of its own: the command gets it without `HELYX_KEEP_` and without the first character of its value. The watchdog touches no other name, so such a variable cannot replace `PATH` or `HOME`. A kept value has a `=` in front, which the watchdog takes off, because the port takes an empty value for "remove", and an empty `PERL_UNICODE` is not the same as none (perl reads it as `-C` with no number). One limit is accepted with no ticket, because a `PERL*` variable that is not UTF-8 is rare. The VM decodes a name or a value that is not UTF-8 as Latin-1. The command gets such a `PERL*` value with each byte from `0x80` as two bytes. A `PERL*` name that is not UTF-8 stays in the watchdog's environment, and the command gets a second variable with the decoded name. perl reads no such name, so the watchdog is safe. Before #71 these bytes reached the command as they were. A perl that finds its POSIX module only through `PERL5LIB` cannot run the watchdog; no such perl is known, because POSIX is a core module. Other variables are not touched. `PERL_BADLANG` stays in the watchdog's environment. It only stops perl's locale warning. Without it, a user with a bad locale and `PERL_BADLANG=0` gets the warning in front of every result, and a warning over the preamble limit stops every command. Locale variables stay, because they change only perl's startup text, which the marker read handles (row above). A `perl` first in `PATH` that is not perl, and a command that attacks its own watchdog on purpose (same user ID: it can kill or trace the watchdog), are out of scope. Text from the environment (a locale warning) cannot forge a marker |
| bash command start after the group marker | The watchdog can die between the marker and the go-ahead (a `kill -9` from outside). The child can end before or at the `exec` of bash: bash was removed after it was found, or the child dies by `die`. The start line is `<nonce> 1`. The failure report is `<go> 0` and then the reason. The watchdog reads at most 4,096 bytes of reason in one read. The reason is the path of bash and an OS error text, or a `die` text of perl. These are far under that limit. The reason goes through the tool result truncation. `<go>` is 8 random bytes for each call. It is the go-ahead line on the watchdog's stdin, so no argument list and no child holds it. The read of the report pipe has no wait. The watchdog reads it only after the child ended, when the `exec` or the exit has closed the write end | closed, ticket #70. A result is ok only when the start line is the first text after the go-ahead and the output holds no failure report. The held child writes the start line as its last act before the `exec`, so the line is in front of all command output. A watchdog that dies before it passes the go-ahead on leaves no start line. The child it held gets end of file and exits without the command. The child reports a failed `exec`, or its own death by `die`, through a pipe that closes on `exec`. The watchdog reads end of file when the `exec` worked. It reads the error when the `exec` did not work, and then it writes the failure report. The watchdog runs without the perl variables of the environment (row above, #71), so every handle is binary and every reason is bytes. The `binmode` of the report pipe and the encode of the reason, which #70 added against `PERL_UNICODE`, are removed: the tests of #70 for `PERL_UNICODE=A` and for a path with characters of two and three bytes pass without them. The report counts wherever it is in the output, so text of perl in front of it cannot hide it. #71 removed the one known source of such text (`PERL5OPT=-w` gave a warning about the `exec`); the rule stays because it fails to the safe side. The result is an error: `the command did not start: cannot run <bash>: ...`, the `die` text, or `the command gave no start line: ...`. A command that ran can read the nonce from the process table, but not `<go>`, so it cannot write a failure report. A real exit status 127, 137, or 255 stays an ok result. Output that was cut in the buffer (row "bash output buffer while the command runs") has lost the start line and is an ok result. The watchdog's own text cannot reach the 409,600 bytes where the cut starts, so a command ran. A `kill -9` of the child after the start line and before the `exec` gives an ok result with `Exit code: 137`. This is not a hole. No observer can tell it from a kill at the first instruction of bash. In both cases no part of the command ran, and the result says that a signal ended it. A second exception of the same kind exists. The `exec` fails, and a `kill -9` from outside ends only the watchdog before it reads the report pipe. The start line is there and the failure report is not, so the result is ok with `Exit code: 137`. This needs two faults at the same time in a window of milliseconds. No part of the command ran, and the result says that a signal ended it. An executable file `bash` with no `#!` line, first in `PATH`, is not a failed start. The `exec` gets `ENOEXEC`, and `execvp` in libc then runs the file as a script of `/bin/sh`. POSIX specifies this. The file that the user's `PATH` names as bash ran, so the result is ok with its output and exit status. A command that attacks its own watchdog on purpose is out of scope: it has the same user ID, so it can kill or trace the watchdog, and on Linux it can open the report pipe again through `/proc`. Closed by #71 (row above): the lost reason of a failed `chdir` with `PERL_UNICODE=A`, and the marker read that had no end with `PERL5OPT=-d`. The marker read has no time limit. With a real perl it ends, because the watchdog writes the marker before it reads anything, and perl without the `PERL*` variables reads nothing before the watchdog runs. A program `perl` first in `PATH` that writes nothing and never exits holds the call until the turn abort, which kills the registered watchdog group: unbounded, and accepted with no ticket, because a false `perl` is out of scope |
| wait for a bash command | unbounded; abort ends it (the bash tool's `ponytail:` marker) | the turn abort kills the command's process group |
| wait for a killed process group | 500 ms TERM grace, then KILL, then a 5 s ceiling of real elapsed time per wait; command groups are swept first, then watchdogs, which get one 5 s window to exit by themselves and, if they do not, a KILL with a further 5 s ceiling | a group that survives KILL past the ceiling is stuck: the result is an error, `cancel/2` reports the failure, and the hands refuse later tool calls with an error result while the group lives; chat, abort, and quit are not blocked |
| session provider Task pid set | one pid per turn; a pid whose exit arrived is removed, a pid whose exit was consumed by an abort stays, so the set grows by one pid per abort | accepted for checkpoint one: entries are one pid each and aborts are human-scale |

## Ownership

When anything above a command dies, the command stops. Two rules cover every path (ADR 0004): inside the VM, work is linked to its owner, and the owner traps exits so a crash below it stays a message; at the OS boundary, a command's life is tied to its port through the perl watchdog. perl is required; the bash tool's `check/0` reports a system without it when the hands start, and the session fails to start with a clear error.

| Resource | Created by | Held by | Released on normal end | Released when the holder crashes | Released on abort |
| -------- | ---------- | ------- | ---------------------- | -------------------------------- | ----------------- |
| provider stream Task | session | session, linked | reply consumed, monitor flushed | the link kills the Task with the session, even through an untrappable kill | `Task.shutdown(:brutal_kill)` |
| tool Task | hands | hands, linked | result delivered | the link kills the Task with the hands; a Task's own crash becomes an error result | `cancel/2` brutal-kills the turn's Tasks |
| command port | bash tool, inside the tool Task | the Task | closes when the watchdog exits; the tool closes it itself when the stream gives no marker (#52) | closes when the Task dies, however it died, down to a `kill -9` of the whole VM | closes when the Task is killed |
| command process group | the watchdog's fork; the id is registered with the hands before the command may run | hands (the id) and watchdog (the life) | the hands KILL the group at delivery and wait until it is gone | the closed port ends the watchdog's stdin: TERM, 500 ms grace, KILL, reap | `cancel/2` TERMs and KILLs the registered groups and returns only when every one is gone |
| start report pipe (#70) | the watchdog, before the fork | the watchdog (read end) and the held child (write end) | the `exec` closes the write end; the watchdog reads the pipe one time when the child ended by itself, and the exit of the watchdog closes the read end | the watchdog and the child exit on the paths of the rows "command process group" and "watchdog process", and the exits close both ends without a read | the same |
| watchdog process (its own group) | `Port.open` in the bash tool; registered with the hands as `:watchdog` | the port | exits after it reaps the command | the closed port is its signal; it ignores TERM, and the hands sweep it only after every command group is gone or stuck, so no KILL from the hands cuts the reap short | the hands wait for it, so an abort cannot return while the command is unreaped |

A call can register several groups; the hands hold them as a set per Task, and every one is cleaned. A group that survives KILL is remembered as stuck (see the bounds table). A child that leaves its process group on purpose with `setsid` stays a documented limit.

## Transcript and events

- Message shape and session file format: see ADR 0001 and the section below.
- Session files live under `~/.helyx/sessions/<project>/<session>.jsonl`.
- Eleven events: `agent_start`, `agent_end`, `turn_start`, `turn_end`, `message_start`, `message_update`, `message_end`, `tool_execution_start`, `tool_execution_end`, `queue_update`, `model_change`. Each carries the session id, the turn id (nil on the queue drain between turns and on every `model_change`), and a sequence number.

### Session file

One JSON object per line. Every entry has `id`, `parent_id`, `ts`, and `type`. The first entry is the header and its `parent_id` is null. Example entries, one per kind:

```json
{"id":"01J...","parent_id":null,"ts":"2026-09-17T10:00:00Z","type":"session","version":1,"cwd":"/path/to/repo","model":"opencode-go/kimi-k2"}
{"id":"01J...","parent_id":"01J...","ts":"...","type":"message","role":"user","content":[{"type":"text","text":"List the tests."}]}
{"id":"01J...","parent_id":"01J...","ts":"...","type":"message","role":"assistant","model":"opencode-go/kimi-k2","stop_reason":"tool_use","usage":{"input":120,"output":40},"content":[{"type":"text","text":"Listing."},{"type":"tool_call","id":"call_1","name":"bash","arguments":{"command":"ls test"}}]}
{"id":"01J...","parent_id":"01J...","ts":"...","type":"message","role":"tool_result","tool_call_id":"call_1","tool_name":"bash","is_error":false,"content":[{"type":"text","text":"core_test.exs"}]}
{"id":"01J...","parent_id":"01J...","ts":"...","type":"model_change","model":"claude-code/opus"}
{"id":"01J...","parent_id":"01J...","ts":"...","type":"harness_session","provider":"claude-code","harness_session_id":"..."}
```

Content blocks: `text` has `text`. `thinking` has `thinking` and an optional `signature`. `tool_call` has `id`, `name`, and `arguments`. `image` has `mime_type` and base64 `data`.

Rules:

- The header carries `version`. A reader rejects a version it does not know.
- The format owns a closed `stop_reason` set: `end_turn`, `tool_use`, `max_tokens`. A reader decodes them without touching the atom table's history, so a fresh VM resumes a saved file. A terminal event with a stop reason outside the set, or a usage the file cannot encode, fails the turn as a malformed stream event before the message exists. A new stop reason is a format change.
- A tool result links to its call by `tool_call_id`. A tool call with no result after the turn ended gets an `aborted` error result on resume (see Abort).
- Streamed partial messages are not written. Only the completed message is appended, after the provider finishes it.
- A file is read line by line. A last line that does not parse is a torn write. On open, the file is truncated to the end of the last line that parses before anything is appended.
- On resume, the leaf is the last entry in file order.
- A reader never raises at the caller. A header with an unknown or missing version, and an entry with a shape the writer never produces, come back as `{:error, reason}`. Only the last line can be repaired: a bad line mid-file is a malformed file and is rejected without truncation. A repair that cannot write, and a sessions directory that cannot be written on create, are their own error classes.
- Provider deltas that are not valid UTF-8, and tool calls whose fields the file cannot encode, fail the turn as malformed stream events; prompts that are not valid UTF-8 are rejected at the client. Transcript text is valid, and every value in it survives the round trip to the file, from the moment it exists.
- Tool output is scrubbed to valid UTF-8 when it becomes a tool result message, so every consumer of the transcript, the file and the providers, sees valid text. Invalid bytes become replacement characters.
- A write failure mid-session logs a warning, turns persistence off for that session, and the turn continues on the in-memory transcript.

Bounds:

| What | Bound | Over the bound |
|---|---|---|
| Error line of `mix helyx` | One line. Text from the file goes through `inspect/1` with its default limits, or is an exception message; the `inspect/1` limits count characters, so the longest line measured is about 16.5 KB, from a version string of 4-byte characters in a header of at most 65,536 bytes. The clean pass is linear in the text | No hard cap on the length. Control characters and bytes that are not UTF-8 become `?` |
| Session file on resume | One regular file of at most 67,108,864 bytes (64 MiB), read whole into memory; the read stops one byte past the limit, so a file that grows during the resume is still over it. The limit is on file bytes. The lines are parsed one at a time, never split into a list of all lines. The heap that the JSON decode of the file uses is open, ticket #64: measured at 12 to 42 bytes of heap per file byte, for many short entries, for arrays of short values, and for deep nesting in one line. Decoded strings can keep the file binary alive for the life of the session | `{:error, {:too_large, text}}`; the text names the limit and says to start a new session; the file is not mutated. A last entry that lacks its newline counts with it, so a repair never makes a file that the next resume rejects. Compaction bounds the transcript itself (#1) |
| Header scan for the most recent session | The first 65,536 bytes of at most 256 regular `*.jsonl` files in the project directory. The scan takes the files with the newest modification time. The path breaks a tie. Each regular file uses one of the 256 places, also a file that is not a session and a session of a different directory with the same slug. A header line that does not end inside the 65,536 bytes is not read whole. The directory listing and one `stat` per listed file have no bound on the count, one file per session started there; they read no file content. The triage of #62 accepted this for checkpoint one; measured at 948 ms for 20,000 files. Each scanned file gets a second `stat` before its open, and the resumed file gets a third before the full read | A longer header line is cut, does not decode, and the file is not a session; a session older than the 256 newest files is not scanned, and the result is `:not_found` when no scanned file has the working directory; a pipe, a device, or a directory is skipped without an open; the window between the type check and the open is open, ticket #65 |
| Project directory slug | Last 100 characters of the slugged cwd | Collisions are disambiguated by the header `cwd` |
| Prompt text | Must be valid UTF-8 | `{:error, :invalid_utf8}` at the client boundary |
| Steer and follow-up queues | 32 entries each; entry text is human input, size accepted as unbounded (#29) | The session returns `{:error, :queue_full}` |
| `cwd` on create | Must be valid UTF-8. `SessionFile.create` checks the model too, but a session rejects a bad model earlier, see the model ref row | `{:error, {:create_failed, :invalid_utf8}}` |
| Model ref (`--model`, `Session.set_model/2`, the `/model` argument, the model of a resumed file) | Valid UTF-8, at most 256 bytes, `provider/model` with both parts present, no whitespace and no Unicode category C character (control, format, unassigned), so a printed ref carries no terminal control sequence. Characters that show as nothing (U+3164, U+2800, a variation selector) are accepted, so two refs that look the same can differ; the effect is cosmetic | `{:error, {:invalid_model_ref, ref}}`; the error holds the caller's own string, and a TUI notice shows at most the provider id, never the whole ref |
| Event text into the composer (a key code, a paste) | Must be valid UTF-8. The input widget raises `ArgumentError` on other text, and a raise in a callback stops the TUI process. One private function in `Helyx.TUI`, `edit/3`, is the only path from event text to the widget, and it checks that the text is a binary and that `String.valid?/1` is true before the call. Exception: `text_input_set_value/2` gets only the literal `""`. The terminal backend gives a Rust `String`, so no known source sends such text today; the guard protects the process if a source appears. Size is not checked: the composer is unbounded human input (#29) | The TUI rejects the full text and does not repair it: a replacement would send text the user did not type. The composer does not change, and the status bar shows the reason `input rejected: not valid UTF-8` until the next key press or paste (#46). The reason is one field, so many rejected events use no more memory than one |
| `/model` command line in the composer | A line is a command line when it starts with `/model` after any run of Unicode whitespace and category C characters (a pasted BOM, a zero width space). A command line is never sent as a message. The word must end the line or be followed by at least one such character; the rest of the line, trimmed, is the ref. The input widget drops control characters from a paste, so a pasted tab is not a separator. The TUI reads only the head of the line and does not split or measure the rest: the composer is unbounded human input (#29), and the model ref row bounds the argument, size first | No ref, or no separator after the word (`/models`, `/modelfake/x`), shows `usage: /model provider/model`; a ref with whitespace or a category C character inside, or over the size, is an invalid model ref; the text stays in the composer. A prompt cannot start with `/model`. The rule does not cover looks: a line with an invisible character inside the word (`/mo` U+200B `del`), an invisible character outside category C before it (U+3164, U+034F, a variation selector), or a homoglyph is a message and goes to the model as a prompt. No pattern closes that class; accepted, the cost is that of a typo, and no trust boundary is crossed. A category C character after the ref is not trimmed, so such a ref is an invalid model ref |
| TUI cell list and the search for an open tool cell (`Helyx.TUI.ViewModel`) | The cell list has no limit of its own. It grows by one cell for each message, tool call, and notice, so the session and the human bound it. Each `tool_execution_end` event makes one pass over the cells to find the newest open tool cell with the same `tool_call_id` (#83). The cost is linear in the cell count for each tool result, the same order as the append of a cell. The size is accepted as unbounded, like the composer (#29); a limit on the cell list: accepted for checkpoint one, with no ticket. The reason: the render of the transcript is also linear in the cells on each frame, so the search adds no new order of cost | Nothing is cut. A result that matches no open tool cell is ignored and does not raise. A cell that has a result never changes |
| Model change entries per session file | One per accepted switch, human-paced, each at most about 650 bytes (a 256-byte ref of `"` or `\` characters doubles in JSON, plus `id`, `parent_id`, and `ts`); the count is bounded only by the 64 MiB file limit on resume | See the session file row |
| SSE line from the model gateway | 1 MiB, terminated or not; the buffer overshoots by at most one transport chunk | One `{:error, {:line_over_limit, limit}}` event ends the stream |
| Tool call bytes per response (argument fragments with a flat charge each, ids, names, entry keys, a flat charge per call) | 10 MiB across all calls, overshooting by at most one line; integer call indexes at most 10,000 | One `{:error, {:tool_call_bytes_over_limit, limit}}` event ends the stream; no partial call is emitted |
| HTTP error response body | 16 KiB; the accumulator overshoots by at most one transport chunk | The body is cut to valid UTF-8 at the limit and marked `[truncated at the N-byte limit]`; the rest of the response is cancelled |

The three provider limits carry no research citation: `docs/research/coding-tools.md` covers tool output truncation, not gateway input, so the values are set here.

Ownership:

| Resource | Holder | Release |
|---|---|---|
| Session file handle | The calling process; every read and append opens and closes inside one `SessionFile` call | On return; no handle outlives a call |

A `Session.start` that fails after the file is created (a supervisor or hands failure) can leave a header-only session that a later resume restores as an empty transcript with the right model. Open, accepted for checkpoint one; the window is narrowed by creating the file only after the plugins resolve.

Two more holes are open and accepted for checkpoint one. Nothing locks a session file: two runtimes that resume the same file both append from the same leaf and interleave their entries. Local mode is one user in one node; a lock lands with a multi-node transport. And a resumed session starts a new event stream: sequence numbers restart at one, and a client renders from the restored transcript, not from event history.

## TUI

- ex_ratatui, alternate screen. The TUI is `Helyx.TUI` in `plugins/bundled`, defined only when `ex_ratatui` is loaded (ADR 0005). It implements no Core interface and is not in Core's plugin list: it is a client that subscribes to one session. Local delivery is OTP messages from `Helyx.Session.subscribe/1`; a Transport interface arrives with the first remote client.
- The view model is a pure fold over events, `Helyx.TUI.ViewModel`, tested with scripted event lists. It grows with the conversation, bounded by the session. A tool result attaches to the newest open tool cell with the same `tool_call_id`, wherever the cell is (#83). A notice can arrive while the tool runs. The session runs tool calls one at a time. The fold does not depend on that order. Only a message with the role `tool_result` and a binary call id attaches. Any other message changes no cell. The composer is human input; its size is accepted as unbounded, like a queue entry's text (#29). Tool results render at most four content lines each, plus one truncation row naming the hidden line count.
- Escape aborts. Enter sends a steer during a turn and a prompt when idle. Alt plus Enter queues a follow-up: most terminals cannot tell Shift+Enter from Enter without the kitty keyboard protocol, so Alt is the modifier. The status bar shows the model, the run state, and the queue counts. When the client rejects input, the status bar also shows the reason in red, as ` ✕ <reason> ` at the start of the line: `not sent: the queue is full` or `not sent: not valid UTF-8` for a steer or a follow-up, and `input rejected: not valid UTF-8` for event text. The rejected text stays in the composer. The reason is the `reason` field of the view model. It is client-local, like a notice: `ViewModel.reject/2` sets it, and no event changes it. Each reason is a fixed text of `Helyx.TUI`, so it holds no input and its longest text is 31 bytes. The reason comes first because the line does not wrap and a model ref can be 256 bytes; the reason is at most 35 columns with its mark, so it shows in full at 35 columns or more. The next key press or paste clears the reason before the TUI handles that input, so the same input can set a new reason. A new reject replaces the reason, also when it comes from a key repeat, because the newest reject is the one the user must see. The release and the repeat of a key do not clear it, because those of the rejected Enter come immediately after the reject. There is no terminal bell. A rejected `/model` switch stays a notice cell, because its text must stay visible while the user corrects the ref.
- `/model provider/model` in the composer switches the model through `Session.set_model/2`, idle or during a turn. It is the only command. The rule is on bytes, not on looks: a line that starts with `/model`, after whitespace and category C characters, is never sent as a message, and any other text is a message; see the bounds table for the exact rule and its limit. The status bar changes when the `model_change` event arrives, not when the call returns, so the view model stays a fold over events. A rejected switch adds a notice cell (`ViewModel.notice/2`, client-local like the composer) and keeps the text in the composer.
- Queued steers are delivered together at the next provider call. On a harness turn, a steer aborts and resends (see Harness turns).
- The `helyx` Mix task lives in `apps/coding_agent`. `mix helyx [directory] [--model provider/model] [--resume]` starts Core with the bundled plugins, one session in `directory` (default: the current one), and the TUI, and quitting restores the terminal. The session is written under `~/.helyx/sessions`; `--resume` continues the most recent session for the directory with its saved model, so it does not combine with `--model`. The scan for that session reads only the 256 files with the newest modification time in the project directory, and an append sets that time. A session older than those files is not found, and the task prints the `:not_found` sentence, `no saved session for this directory; start one without --resume`, although the file exists. A start or a resume that fails prints one line on stderr, `could not start the agent: <sentence>`, and the task exits with a non-zero status. `CodingAgent.error_text/1` is the one place that makes the sentence. It has a clause for each shape of `Helyx.SessionFile.error/0`, for the model ref and provider errors, for `{:tool_unavailable, name, reason}`, and for `{:terminal_init_failed, text}`. The `:too_large` text prints as it is. An invalid model ref stays out of the sentence, as in the TUI notice. A value from the file (`:unknown_version`, the type of a bad entry, a provider id) prints through `inspect/1` with its default limits, so a JSON object shows as a map. A bare atom prints as its POSIX text when it is a POSIX code. An error with no clause prints through `inspect/1`, so it can show as a tuple: a duplicate tool name, a supervisor error, a session that died. Every sentence gets one clean pass: a whitespace run becomes one space, and a control character or a byte that is not UTF-8 becomes `?`. The `:invalid_file` text can be an exception message of many lines, and the clean pass puts it on one line. The task reads the sessions directory from the `:sessions_dir` application env key of `:coding_agent`, so that a test uses its own directory. The default stays `~/.helyx/sessions`.

## Out of scope

MCP, sub-agents, permission prompts, plan mode, todos, background shell, compaction, branching, editing queued messages, queue persistence, remote hands, packaging, Anthropic API key provider, dedicated grep, find, and ls tools, skills discovery, replaying the transcript into a fresh harness session. During a harness turn, Helyx tools are not visible to the model (ADR 0002).
