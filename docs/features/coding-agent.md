# Coding agent (checkpoint one)

A terminal coding agent built on Helyx. It is the first product and it proves the substrate.

## Goal

From the TUI, prompt the agent about a repository. It reads, edits, and runs shell commands in a loop. Output streams. Abort, steer, and follow-up work during a turn. The session resumes after a restart. One user: the author, on this repository.

## Shape

- Root `helyx` Mix project. All bundled plugins live in one Mix project, `plugins/bundled` (app `helyx_plugins`), and the product lives in `apps/coding_agent`. Both depend on Helyx by path, and the product depends on `helyx_plugins` and on `ex_ratatui`, which is optional there (ADR 0005). No umbrella.
- Elixir 1.19 and OTP 28, pinned in `.tool-versions`. Started with a Mix task.

## Runtime

- One session process per conversation, started under a DynamicSupervisor and found by id through Registry.
- Each turn has a turn id and runs in a Task under Core's task supervisor. The session monitors the Task and ignores messages from a Task that is no longer current.
- One hands process per session runs tool calls as Tasks and spawns harness programs. See ADR 0003.
- Core starts as a child spec that takes the plugin list. No application config.
- Interface modes: Provider, Tool, and Event are `multi`. ModelContext, Compaction, and Transport are `single`.

### Abort

Abort ends the current turn by id. The session kills the turn Task and tells the hands to cancel that turn id. The hands kill every tool Task for that turn and call the `release/3` of each tool with the handles its Tasks held, with the mode `:cancel` (`docs/features/tool-resource-release.md`). Closing a port does not stop the command behind it, so the bash tool starts each shell command in its own process group and holds it with the hands. Its release sends `SIGTERM` to the group on abort and `SIGKILL` after a short grace period. The hands reply to the session only after every release has returned or passed its deadline, and the session starts the next turn only after that reply. The session does not wait in a call for that reply. It ends the turn and sends the abort events at once. The reply arrives as a message, and `Helyx.Session.abort/1` returns then. Until the reply, the session answers every client call (#93). A prompt, a steer, or a follow-up in that time queues. One turn starts with the queue after the reply. A second abort in that time drops the queue, and it returns with the first. An abort that the session reads after the reply finds the turn that the queue started. It aborts that turn like any running turn, so the user messages of that turn stay in the transcript, as they do for a prompt and an abort with no release. In both orders, nothing that was sent before an abort runs or waits in a queue when that abort returns. Every tool result and harness event carries its turn id, and the session drops any that arrive for a turn that is no longer current. A shell command killed mid-write can leave a half-written file. That is accepted.

When a turn is aborted, by the user or by a restart, every tool call in it that has no result gets a tool result entry with `is_error` true and the text `aborted`. The entry is appended to the transcript, so the next provider call sees a complete call and result pair. Providers reject a tool call without a result, so removing the call is not an option.

### Steer and follow-up queues

Queued steers join the transcript as user messages, in order, before the next provider call inside the turn. Queued follow-ups start a new turn after the current turn ends normally; anything still queued at that point, steers included, becomes that one new turn's prompt, steers first, so no typed message is lost. A steer or follow-up sent with no turn running starts a turn at once, like a prompt, so the client never races the end of a turn. Abort and turn failure drop both queues. Every change emits a `queue_update` event with the counts; the drain at a normal turn end goes out between turns with a nil turn id. Each queue holds at most 32 entries; past the cap the call returns `{:error, :queue_full}` and the TUI keeps the text in the composer and shows the reason in the status bar (#46).

## Interfaces and bundled plugins

| Interface | Plugins in this checkpoint |
|---|---|
| Provider | `OpenAI` (model provider, OpenAI wire format, first target OpenCode Go or Zen), `ClaudeCode` (harness, `claude -p` stream-json over stdio), `Codex` (harness, `codex app-server` JSON-RPC over stdio), `Fake` (scripted, for tests) |
| Tool | `Read`, `Bash`, `Edit`, `Write` |
| ModelContext | `Default`: base prompt plus `AGENTS.md` files from home down to the working directory |
| Compaction | `None`: no-op |
| Event | the TUI subscribes; no bundled handlers |
| Transport | `Local`: OTP messages in one node |

A model is named by one string, `provider/model`. A session parses it once per start, resume, and switch.

### Model switching

`Helyx.Session.set_model/2` switches the model of a running session. The ref goes through the same parse and the same provider lookup as the `:model` of `Session.start`. These are the errors (`t:Helyx.Session.model_error/0`):

- A bad ref returns `{:error, {:invalid_model_ref, ref}}`.
- An unknown prefix returns `{:error, {:unknown_provider, id}}`.
- A prefix that two plugins share returns `{:error, {:ambiguous_provider, id}}`.
- A provider with a bad `turn/0` returns `{:error, {:bad_provider_turn, id}}`.
- A provider plugin whose `id/0` raises, throws, exits, or returns a value that is not a binary returns `{:error, {:bad_provider_id, module}}` for any valid ref (#153).

In every case the model, the file, and the event stream stay as they were. An accepted switch does three things inside one session call: it appends a `model_change` entry to the session file, it replaces the session's model ref and provider module, and it emits a `model_change` event with the new ref. A switch to the current model is accepted and recorded like any other.

A turn fixes its model and provider when it starts. A switch during a turn is accepted at once, but every provider call of the running turn, and the `model` of its assistant messages, stay on the old model. The next turn, a queued follow-up too, uses the new one. The switch belongs to no turn, so its event carries a nil turn id, during a turn too.

A `model_change` append that fails on disk is treated like a message append that fails: a warning, persistence off for the session, and the switch still holds in memory.

Both directions between a model provider and a harness provider go through this one path, because the session sees only a provider module. A switch to a provider with an external turn starts a fresh harness session that gets the transcript (see Harness turns).

### Harness turns

A harness runs its own loop and its own tools. Helyx starts the program for the turn, translates its output into messages and events, and records them in the transcript. Helyx tools are not visible to the harness (ADR 0002).

- Steer: a harness does not accept a message inside a turn. On an external turn, a steer queues and aborts the turn, and when the hands have released the program, a new turn starts with the queued messages. The provider's prompt is every user message at the end of the transcript, so a prompt the harness did not answer is sent again with the steer. The transcript keeps whatever the harness completed before the abort.
- Turn: a provider's optional `turn/0` returns `:external`; the default is `:local`. The session calls `Helyx.Provider.turn/1` once, in the caller of a start, a resume, or a switch, and keeps the result; a `turn/0` that raises, throws, exits, or returns any other value fails that call with `{:bad_provider_turn, id}`, so no session runs plugin code for the turn. The stream of an external turn runs as a Task of the hands with the provider module in the place of the tool, so the provider holds the process groups of its program with `Helyx.Tool.hold/1` and implements `release/3` (ADR 0004). Its terminal reaches the session as `{:stream_end, turn_id, terminal}` after the release. While a handle is unconfirmed, the hands refuse the stream of an external turn like a tool call; the stream of a local turn is not a Task of the hands and is never refused.
- Stream: besides the deltas and tool calls, a harness stream sends `{:message_end, stop_reason, usage}` for each assistant message whose tool calls ran, and `{:tool_result, call_id, {status, text}}` for each result. The session appends each message and each result as it arrives, with the events of a model turn: `message_end`, `tool_execution_start` for each call, `tool_execution_end` for each result. No message goes between a call and its result: before the session appends a message, each call of an earlier message with no result gets an `aborted` error result. A result for a call of no completed message, or for a call that got its `aborted` result, is dropped. So is a result whose id matches no open call, for example an id that is not valid UTF-8: the session uses the id only to find the call. At the end of the call, when the turn fails, and on an abort or a steer, each call with no result gets an `aborted` error result. A result that arrives, or is aborted, after the next message has started to stream has its `tool_execution_end` event after that message's `message_start` and the deltas sent before it; the transcript order is as stated. The session never runs the tool calls of an external turn on the hands.
- Resume: each harness issues its own session id. The provider reports the id of each fresh harness session as `{:harness_session, id, cut}` when the program's `init` line arrives (Claude Code) or `thread/start` answers (Codex). The session writes it to the transcript as a `harness_session` entry, keeps the last id of each provider (restored on resume), and emits a `harness_session` event, `%{provider, harness_session_id, lost, cut}`. A turn passes the stored id as `:harness_session_id` only when the last assistant message of the transcript came from this provider after that harness session's entry; otherwise it passes nil. A message of the harness session shows that it read the whole replay and the prompt. A fresh session that ends before its first message (an abort, a steer, a failure, or a restart) is not resumed, and the next turn replays again. The session keeps the number of messages before each provider's last entry. On resume, the file gives this number back. If the harness no longer has the session, the provider starts a fresh one in the same call, and `lost` is true. The TUI shows a notice that the harness lost its own context.
- Replay: a fresh harness session (the first harness turn, a lost session, or a switch from another provider) gets the transcript before the prompt. For Claude Code, the provider sends the history over stdin as stream-json lines: one `assistant` line per assistant message (text and tool calls; thinking is not replayed), and one `user` line with `shouldQuery: false` for each run of user messages and tool results, so no replayed line starts a model call. Tool call ids get every character outside `[a-zA-Z0-9_-]` replaced with `_`. The map is not one to one: two ids that differ only in such characters (`a.1` and `a_1`) become one id in the replay, and the program can pair a result with the wrong call. Provider ids are generated and unique in practice, so this is accepted with no ticket for checkpoint one. The prompt is the user messages at the end of the transcript. The replay keeps the newest lines within 400,000 bytes and starts at a line with no tool result, so every kept result keeps its call; `cut` counts the messages left out, and the TUI shows a notice when it is above 0. The facts are in `docs/research/claude-code-stream-json.md`. For Codex, the provider sends the history with one `thread/inject_items` request before `turn/start`; it makes no model call. Each message gives Responses API items: a user message an `input_text` message, an assistant message an `output_text` message per text and a `function_call` per tool call (thinking is not replayed), and a tool result a `function_call_output`. A call id that is not 1 to 64 characters of `[a-zA-Z0-9_-]` becomes `h_` and 62 hex digits of its SHA-256, the same for the call and its result; a tool name gets every other character replaced with `_` and is cut at 64, the function name limit of the Chat Completions API; that limit is not verified for `thread/inject_items` (research note). The same 400,000-byte cap and start rule apply, counted on the JSON of the items (`HarnessIO.cap_replay/2` in `plugins/bundled/lib/helyx/harness_io.ex`, shared by both providers).
- Claude Code runs `claude` from `PATH` with `-p --output-format stream-json --verbose --include-partial-messages --input-format stream-json --permission-mode bypassPermissions --model=<model>`, and `--resume=<id>` for a resume, under `Helyx.Watchdog` in the session's working directory. Its stderr is dropped; the `result` line carries the errors.
- Codex runs `codex app-server` from `PATH` under `Helyx.Watchdog` in the session's working directory, with open input (row "watchdog input on stdin"), and speaks JSON-RPC lines (#11). One run is one turn: `initialize`, then `thread/resume` with the stored id, or `thread/start`, then `turn/start` with the prompt. A resume error `no rollout found` is a lost thread: the same run starts a fresh thread. Threads run with the approval policy `never` and the sandbox `danger-full-access`, the trust of the bash tool. A command or file change approval request that still arrives is answered `accept`; every other server request gets the JSON-RPC error -32601. After `turn/completed`, Helyx ends the program's input, so the program exits by itself within the exit wait. A tool item (a command, a file change, an MCP tool call, and the other item types in the research note) is a tool call when it starts and a tool result when it completes; only the notifications of the run's own thread count. The first result of a message's calls closes the message with `message_end`. Codex runs tool items side by side, so a call of a closed message can still run when the next message closes: the provider then holds that `message_end`, and every event after it, until the earlier calls have their results; a result of a call of a sent message goes out at once, a held result goes right after its own `message_end`, and any terminal (the turn's end, an error, the program's exit, a line over the cap, the held events over their cap) sends what is held, so the session aborts only calls that never got a result. An abort or a steer is not such a terminal: it drops the held events, finished results included; the held messages and their calls never reach the transcript, and only the calls of sent messages get `aborted` results. Accepted, #112: the session closes the turn when the abort starts, so no event of the stream reaches it after that; keeping those events needs a session change (`docs/features/external-turn.md`, section "Known gap"). At most 10,000 events are held (row "Codex held events"). `Helyx.Provider` states the rule for every harness provider. The stream Task traps exits: on an abort or a steer, the hands' `:shutdown` makes it send `turn/interrupt` and wait up to 1,000 ms for the turn end, within the 2,000 ms shutdown grace of the hands, before the release. The interrupt needs the turn id, so an abort before the `turn/start` answer sends none; the release's TERM then stops the program. The facts are in `docs/research/codex-app-server.md`.
- Stream Task shutdown (#11): the cancel of the hands stops a harness stream Task with `Task.shutdown/2` and a 2,000 ms grace (`@stream_stop_ms` in `lib/helyx/session/hands.ex`), then releases its handles; a tool Task is still brutal-killed. A stream that does not trap exits dies at once, as before.

## Bounds

| What | Bound | Over the bound |
| ---- | ----- | -------------- |
| file read (read, edit, model context) | one regular file, at most 10,485,760 bytes, valid UTF-8 | a device, a directory, or a larger file is an error; invalid UTF-8 is an error, `binary file, N bytes` |
| tool result text | 2000 lines or 51,200 bytes of line content, on whole lines | read keeps the head and bash keeps the tail, except a no-marker start error, which keeps its first 2,000 bytes (row "bash watchdog marker read"); the result says which absolute lines it shows, and a truncated head names the offset that continues the read, when lines follow the shown lines. When the line at the kept edge (the first of a head window, the last of a tail) is over the byte cap by itself, it is cut to the cap and is the only line shown; the notice names the line and the bytes the tool kept (`, line N cut at B bytes`), and no offset reaches the rest of that line. When the cut line of a head window is the last line of the text, the notice has no `read again` clause, because that offset returns nothing from `truncate/3` (#61), and it is an error from the read tool (#78). Only the bytes at the cut edge decide what the cut loses (#68): the cut loses the fewest bytes, 0 to 3, that leave a whole character at the edge, so the shown line is 51,197 to 51,200 bytes. That removes a part of a character (a cut inside one, or a prefix that no valid character has, such as `0xC0` or `0xED 0xA0`) and up to 3 invalid bytes that stand at the edge, such as a `0xFF` or continuation bytes with no lead byte. When no loss of at most 3 bytes leaves a whole character there (text that is all `0xFF`, four continuation bytes), the edge loses 3 bytes and stays invalid. Invalid bytes anywhere else in the line, such as a partial character at the far end of `head -c` output, do not change the cut. The hands replace every invalid byte that stays. The cut is an accepted ceiling (#50 triage), and opencode's read also marks a cut line (`docs/research/coding-tools.md`, opencode). Line numbers in a bash result count from the kept output buffer (next row). B counts the tool's own text: when the hands replace invalid bytes afterwards (row "tool result text validity"), the delivered line can be up to three times B. |
| read tool `offset` | a positive integer, a 1-based line number. A missing or `null` offset is line 1. A float with no fraction, such as `2001.0`, is its integer, because the JSON encoders of some models send it; this holds for a float of any size. The tool has no `limit` argument, and it ignores a `limit` key like any key that the schema does not name (#75). pi and opencode both have `offset` and `limit` (`docs/research/coding-tools.md`, pi and opencode). A `limit` argument needs a line count in `Helyx.Text.truncate/3`, which #75 does not change. This is accepted: offset and the truncation notice reach every line, so the tool needs no `limit`. An offset after the last line is an error result, `offset N is after the last line: PATH has T lines` (#78, closed), so an offset that is too large does not look like an empty file. The ticket preferred the error. The tools research records no tool that reports an offset after the last line (`docs/research/coding-tools.md`), so the choice has no source to cite. Every such offset gives an empty window from `Helyx.Text.truncate/3`. Thus only an empty window after line 1 pays for the count. The count is one pass over the bytes that builds no list and does not split the text again, so its memory does not grow with the file. It uses the line rule of `truncate/3`. One trailing newline ends the last line. More trailing newlines are blank lines that count. An offset at a trailing blank line is thus an ok result that can be empty; the last line that a truncation notice can name is such a line at most, never a line after the last one. The empty file is the one exception to the line rule: it has 0 lines here, and `truncate/3` counts 1. Thus offset 1 or no offset gives an empty ok result, and a larger offset is the error. The error shows an offset above 1,000,000,000 as `over 1000000000`, because no file within the 10 MiB read limit has that many lines, so the text does not grow with the digits of the argument. For the same reason `truncate/3` gets at most that offset, so the time of the read does not grow with the digits. PATH is the `path` argument as the model gave it, as in the older `cannot read PATH` error. Its only bound is the 10 MiB limit on tool call bytes | an error result before the file is read, `offset must be a positive integer (a 1-based line number), got K`, never a silent default. K names the kind of the value (`a string`, `an integer below 1`, `a number that is not a whole number of 1 or more`, `a boolean`, `an array`, `an object`) and never shows the value, so the size and the cost of the text do not depend on the argument; the longest text of this error is 111 bytes. This bound is on the tool only. The session bounds the JSON encode cost of a large integer argument for every tool: see the row `Integer in tool call arguments and in the usage of a provider call` (#79). The other tools (`bash`, `edit`, `write`) have only string arguments, and a wrong type there is already an error result from the catch-all clause of `run/2` |
| bash output buffer while the command runs | the last 204,800 bytes | older output is dropped, the result says so; a byte cut that lands inside a character drops the rest of that character, so the kept tail of valid output starts on a character boundary and a cut keeps 204,797 to 204,800 bytes (#51); output that was never valid also loses at most 3 leading continuation bytes and stays invalid for the next row |
| tool result text validity | valid UTF-8 before it enters the transcript | the text is repaired at its two boundaries, before the session sees the result: the hands repair the output of a tool, and `Helyx.Session.Stream` repairs a Claude Code or Codex result. The repair replaces each invalid byte sequence with one U+FFFD (`String.replace_invalid/1`). It runs after a tool's own truncation, or after the stream's 65,536-byte check, and can grow the text to at most three times its byte size. `Helyx.Message` and the TUI do not repair the text again |
| bash command and working directory | no NUL byte; port arguments are NUL-terminated C strings that would cut the string silently. The session checks the working directory at start and resume (row `cwd` at session start), so the hands never pass one with a NUL byte. The bash tool checks both again, because its `run/2` is a public plugin entry and its own boundary | an error result before the port opens; nothing runs |
| bash working directory at command start | must be a directory the watchdog can enter; the hands check it, but it can vanish or be empty afterwards | closed by structure (#52): the port has no cd option; the watchdog does the `chdir` itself before it forks, and a failure writes the marker with `0`, which is never a group id, then the OS reason. The command is held until the go-ahead, so no command output can come before the marker or pass for it. A `pipe` or `fork` that fails in the watchdog uses the same marker (it was exit status 91 in an ok result). The reason carries the directory as given and goes through the tool result truncation. The result is an error, `the command did not start: cannot enter the working directory ...`; nothing is forked, and a real exit status 2 stays an ok result |
| bash watchdog marker read | the marker line is `<nonce> <number>`, and it must end within the first 4,096 bytes of the merged stream; only a line that ends within that limit counts, so which marker is found, if any, does not depend on how the stream is cut into messages. The nonce is 8 random bytes for each call, and only the watchdog's arguments carry it. Whole lines before the marker can only be perl's own startup output (a bad locale warning prints environment values, which can hold any line but not the nonce); they are kept in front of the output | the only path to the go-ahead and to an ok result is a group marker, held with the hands first. A stream that ends, or whose first 4,096 bytes hold no marker line, is an error, `the command did not start: the perl watchdog gave no marker: ...`, with the text after `the command did not start: ` cut to its first 2,000 bytes as in the harness providers, so the words that name perl stay; `Helyx.Watchdog.start/4` closes the port, so the watchdog kills the child it holds (a child whose watchdog is already dead gets end of file on the go-ahead pipe and exits without the command). No window size decides whether an unheld command runs (#52) |
| bash watchdog, perl variables of the environment | The input is every variable whose name starts with `PERL`, but not `PERL_BADLANG`. perlrun names `PERL_UNICODE`, `PERL5OPT`, `PERL5LIB`, `PERLLIB`, `PERL5DB`, `PERLIO`, and more, and each one can change the interpreter. `PERL_UNICODE` makes a `sysread` or a `syswrite` fatal. `PERL5OPT=-d` starts the debugger on the watchdog's stdin. `PERL5LIB` can replace the POSIX module. The rule is the prefix and not a list, so a variable of a later perl is also covered. A name that is not perl's costs nothing, because every value comes back. The bound on the values is the OS limit on arguments and environment together, as before. Each variable adds 12 bytes to that total, so an environment within those bytes of the limit now fails at the start of perl; not reproduced | closed by structure (#71): the port starts the watchdog without these variables (the `:env` option of `Port.open`), so no `PERL*` variable except `PERL_BADLANG` changes how the watchdog runs. Each value stays in the environment under the name `HELYX_KEEP_<name>`, which perl does not read, so only the same user can see it, as before. The watchdog gives each value its name back in `%ENV` before the fork, which does not change an interpreter that runs already. The command sees the user's value of each variable and no `HELYX_KEEP_PERL` name. The prefix `HELYX_KEEP_PERL` is reserved. The watchdog takes a variable of the user with that prefix for one of its own: the command gets it without `HELYX_KEEP_` and without the first character of its value. The watchdog touches no other name, so such a variable cannot replace `PATH` or `HOME`. A kept value has a `=` in front, which the watchdog takes off, because the port takes an empty value for "remove", and an empty `PERL_UNICODE` is not the same as none (perl reads it as `-C` with no number). One limit is accepted with no ticket, because a `PERL*` variable that is not UTF-8 is rare. The VM decodes a name or a value that is not UTF-8 as Latin-1. The command gets such a `PERL*` value with each byte from `0x80` as two bytes. A `PERL*` name that is not UTF-8 stays in the watchdog's environment, and the command gets a second variable with the decoded name. perl reads no such name, so the watchdog is safe. Before #71 these bytes reached the command as they were. A perl that finds its POSIX module only through `PERL5LIB` cannot run the watchdog; no such perl is known, because POSIX is a core module. Other variables are not touched. `PERL_BADLANG` stays in the watchdog's environment. It only stops perl's locale warning. Without it, a user with a bad locale and `PERL_BADLANG=0` gets the warning in front of every result, and a warning over the preamble limit stops every command. Locale variables stay, because they change only perl's startup text, which the marker read handles (row above). A `perl` first in `PATH` that is not perl, and a command that attacks its own watchdog on purpose (same user ID: it can kill or trace the watchdog), are out of scope. Text from the environment (a locale warning) cannot forge a marker |
| bash command start after the group marker | The watchdog can die between the marker and the go-ahead (a `kill -9` from outside). The child can end before or at the `exec` of bash: bash was removed after it was found, or the child dies by `die`. The start line is `<nonce> 1`. The failure report is `<go> 0` and then the reason. The watchdog reads at most 4,096 bytes of reason in one read. The reason is the path of bash and an OS error text, or a `die` text of perl. These are far under that limit. The reason goes through the tool result truncation. `<go>` is 8 random bytes for each call. It is the go-ahead line on the watchdog's stdin, so no argument list and no child holds it. The read of the report pipe has no wait. The watchdog reads it only after the child ended, when the `exec` or the exit has closed the write end | closed, ticket #70. A result is ok only when the start line is the first text after the go-ahead and the output holds no failure report. The held child writes the start line as its last act before the `exec`, so the line is in front of all command output. A watchdog that dies before it passes the go-ahead on leaves no start line. The child it held gets end of file and exits without the command. A watchdog that dies after the marker can close its stdin while the port is still open (#131). The go-ahead write then gets `EPIPE`: the port closes with the exit reason `:epipe` and sends no exit status. `Helyx.Watchdog.start/4` traps exits for that write. The go-ahead is its own write and the first on the pipe, so the pipe takes all of it or the write fails at once. A go-ahead write that gets `:epipe`, or that finds the port closed, gives `the perl watchdog died before the go-ahead: ...`: an error result of the bash tool, and `{:error, {:not_started, _}}` in the harness providers. The input of Claude Code is a later write. A later write to a dead watchdog (that input, and the lines of Codex) is open, ticket #167. The child reports a failed `exec`, or its own death by `die`, through a pipe that closes on `exec`. The watchdog reads end of file when the `exec` worked. It reads the error when the `exec` did not work, and then it writes the failure report. The watchdog runs without the perl variables of the environment (row above, #71), so every handle is binary and every reason is bytes. The `binmode` of the report pipe and the encode of the reason, which #70 added against `PERL_UNICODE`, are removed: the tests of #70 for `PERL_UNICODE=A` and for a path with characters of two and three bytes pass without them. The report counts wherever it is in the output, so text of perl in front of it cannot hide it. #71 removed the one known source of such text (`PERL5OPT=-w` gave a warning about the `exec`); the rule stays because it fails to the safe side. The result is an error: `the command did not start: cannot run <bash>: ...`, the `die` text, or `the command gave no start line: ...`. A command that ran can read the nonce from the process table, but not `<go>`, so it cannot write a failure report. A real exit status 127, 137, or 255 stays an ok result. Output that was cut in the buffer (row "bash output buffer while the command runs") has lost the start line and is an ok result. The watchdog's own text cannot reach the 409,600 bytes where the cut starts, so a command ran. A `kill -9` of the child after the start line and before the `exec` gives an ok result with `Exit code: 137`. This is not a hole. No observer can tell it from a kill at the first instruction of bash. In both cases no part of the command ran, and the result says that a signal ended it. A second exception of the same kind exists. The `exec` fails, and a `kill -9` from outside ends only the watchdog before it reads the report pipe. The start line is there and the failure report is not, so the result is ok with `Exit code: 137`. This needs two faults at the same time in a window of milliseconds. No part of the command ran, and the result says that a signal ended it. An executable file `bash` with no `#!` line, first in `PATH`, is not a failed start. The `exec` gets `ENOEXEC`, and `execvp` in libc then runs the file as a script of `/bin/sh`. POSIX specifies this. The file that the user's `PATH` names as bash ran, so the result is ok with its output and exit status. A command that attacks its own watchdog on purpose is out of scope: it has the same user ID, so it can kill or trace the watchdog, and on Linux it can open the report pipe again through `/proc`. Closed by #71 (row above): the lost reason of a failed `chdir` with `PERL_UNICODE=A`, and the marker read that had no end with `PERL5OPT=-d`. The marker read has no time limit. With a real perl it ends, because the watchdog writes the marker before it reads anything, and perl without the `PERL*` variables reads nothing before the watchdog runs. A program `perl` first in `PATH` that writes nothing and never exits holds the call until the turn abort, which releases the held watchdog group: unbounded, and accepted with no ticket, because a false `perl` is out of scope |
| watchdog input on stdin (`Helyx.Watchdog.start/4`, #10) | The command reads exactly the byte count that the watchdog gets in its arguments, then end of file. Bytes past the count are read and dropped. The go-ahead line is read one byte at a time, so the input can follow it in the same write. The watchdog keeps the input that the command has not read yet in memory: at most the count, which the caller bounds: the row "Harness replay". The pipe is non-blocking and the watchdog writes it only when select reports it writable, so a command that does not read never holds the watch of stdin, and a closed port still kills the group within the grace period. A write that fails for another reason than a full pipe (the command closed its stdin) drops the rest of the input. The bash tool gives no input: its commands read `/dev/null`, as before. SIGPIPE is ignored in the watchdog only, after the fork, like TERM. Open input (`:open`, #11): the command reads what `Helyx.Watchdog.write/2` sends until a NUL byte, then end of file; JSON has no raw NUL, so Codex ends its input with one. The watchdog keeps what the command has not read: the Codex replay (row "Harness replay") and the prompt, then the small JSON-RPC lines of the run | a command that does not read keeps the rest of the input in the watchdog until it ends or the port closes; nothing blocks. Accepted hole: the watchdog exits when the command it started exits, so a background job that still reads the inherited stdin gets end of file after part of the input (reproduced: 65,533 of 3,000,000 bytes). The release kills that job at delivery with the rest of the group, and the provider's reader is the command itself |
| wait for a bash command | unbounded; abort ends it (the bash tool's `ponytail:` marker) | the turn abort kills the command's process group |
| wait for a killed process group (bash, Claude Code, and Codex release, `plugins/bundled/lib/helyx/watchdog/group.ex`) | `:cancel`: 500 ms TERM grace (5,000 ms for Codex, `@term_grace_ms` in `Helyx.Provider.Codex`), then KILL, then a 5 s ceiling of real elapsed time per wait. `:deliver`: KILL, then the same wait; `Codex.release/3` gives a delivery the `:cancel` sequence. Command groups are swept first, then watchdogs, which get one 5 s window to exit by themselves and, if they do not, a KILL with a further 5 s ceiling. No wait passes the release deadline of the hands (row below), and no `kill` run starts at or after it; a skipped probe keeps the group held. A group is gone only when `kill` reports "No such process", with `LC_ALL=C` | a group that survives KILL past the ceiling, or that is still there at the deadline, is returned as still held: the hands make the result an error, the cancel of the hands reports the failure, and the hands refuse later tool calls with an error result while the handle is unconfirmed; chat, abort, and quit are not blocked |
| wait for an abort (`Helyx.Session.abort/1`, `Helyx.Session.Hands.request_cancel/2`) | `abort/1` is a `GenServer.call` with `:infinity`, so the call has no timeout of its own. The session does not block during the wait: it sends the cancel request with `Helyx.Session.Hands.request_cancel/2`, holds the abort callers, and replies to them when the answer of the hands arrives. The list of held callers has no cap: it has one entry for each blocked caller process, thus one for each Escape press in the TUI during the release. It is emptied at the end of the release. Before #93 the same callers waited in the mailbox of the session. Before the release, the cancel of the hands stops a harness stream Task with a grace of 2,000 ms (`@stream_stop_ms`, #11), so the worst abort is 22,000 ms. The hands run one release Task for each tool, in parallel, with one deadline of 20,000 ms (`release_ms` in the state of the hands). At the deadline they kill the release Task and count its handles as unconfirmed. The worst bash release is 15,500 ms (row above). A cancel request that arrives during the release of a delivered call waits for that release first. Each `kill` run of the bash release is a `System.cmd` with no timeout: unbounded, and accepted with no ticket, because `kill` returns at once | the call does not time out. The hands answer the cancel request with `:ok`, or an error that names the unconfirmed handles, by the deadline. The abort returns only when every release has returned or passed its deadline, so the next prompt starts on a clean working directory. A timeout on the call would let the caller go on while processes of the turn still run |
| session client calls (`Helyx.Session.prompt/2`, `steer/2`, `follow_up/2`, `model/1`, `set_model/2`) | each is a `GenServer.call` with the default timeout of 5,000 ms. Before its call, `set_model/2` runs `id/0` and `turn/0` of the provider plugins in the caller, with no deadline (the Provider id row). No handler of the session waits for a release or for a provider. A turn runs in a Task. An abort waits for a message (row above). `Helyx.Session.Hands.run/3` is sent only when the hands are idle: tool calls run one at a time, a result leaves the hands at the end of its release, and no turn starts during the release of an abort. Measured during the release of an abort of 5,500 ms (#93): each call returned in less than 5 ms, and before #93 each call exited after 5,002 ms. During the release of a delivered call the session never blocked | a timeout exits the caller. No known path reaches it. `Helyx.Session.abort/1` is the one client call with `:infinity` (row above) |
| session to hands calls (`Helyx.Session.Hands.run/3`, `Helyx.Session.Hands.stream/4`) | each is a `GenServer.call` with the default timeout of 5,000 ms. `run/3` and `stream/4` reach idle hands (row above). When a handle is unconfirmed, its handler calls `release/3` with `:retry` for each tool, with one deadline of 1,000 ms for all tools (`@retry_ms` in `lib/helyx/session/hands.ex`). The bash retry is one `kill` run and one probe for each group, with no wait | a timeout stops the session. The retry deadline keeps the handler under the timeout |
| wait to hold a handle (`Helyx.Tool.hold/1`) | a `GenServer.call` to the hands with `:infinity`. The handler only updates a map. The call can wait behind one release of the hands, which the row "wait for an abort" bounds | the call does not time out. The command is held until the call returns (row "bash watchdog marker read"), so nothing runs unheld during the wait. An abort kills the tool Task, which ends the wait |
| session provider Task pid set | one pid per turn; a pid whose exit arrived is removed, a pid whose exit was consumed by an abort stays, so the set grows by one pid per abort | accepted for checkpoint one: entries are one pid each and aborts are human-scale |
| unknown message to the session | none. Only a local process can send one, and every message that Helyx sends to a live session has a `handle_info/2` clause | the message is a bug: the session crashes (`FunctionClauseError`, or `CaseClauseError` while it waits for the release of an abort), and it takes its hands and its provider Task with it. The TUI does the same for an event that does not have a shape of `Helyx.Event` |

## Ownership

When anything above a command dies, the command stops. Two rules cover every path (ADR 0004): inside the VM, work is linked to its owner, and the owner traps exits so a crash below it stays a message; at the OS boundary, a command's life is tied to its port through the perl watchdog. perl is required; the bash tool's `check/0` reports a system without it at session start and resume, before any file or process is created, and the session fails to start with a clear error (row "Tool checks at session start"). The Claude Code and Codex providers check for perl next to their program lookup in `stream/3` (`Helyx.HarnessIO.find/1`); without it the turn fails with an error that names perl, and the session lives. perl can go away after a check, so `Helyx.Watchdog.start/4` handles the failure where perl is used: a failed lookup or spawn, a watchdog that ends with no marker, and a watchdog that dies before the go-ahead (#131), give a result that names perl, never a raise. The no-marker text names the perl watchdog, not a cause: an argv over the OS limit also ends there.

| Resource | Created by | Held by | Released on normal end | Released when the holder crashes | Released on abort |
| -------- | ---------- | ------- | ---------------------- | -------------------------------- | ----------------- |
| provider stream Task | session | session, linked | reply consumed, monitor flushed | the link kills the Task with the session, even through an untrappable kill | `Task.shutdown(:brutal_kill)` |
| tool Task | hands | hands, linked | result delivered | the link kills the Task with the hands; a Task's own crash becomes an error result | the cancel of the hands brutal-kills the turn's Tasks |
| command port | `Helyx.Watchdog.start/4`, called by the bash tool inside the tool Task | the Task | closes when the watchdog exits; `start/4` closes it itself when the stream gives no marker (#52); it also closes when the go-ahead write finds the watchdog dead, a failed start (#131) | closes when the Task dies, however it died, down to a `kill -9` of the whole VM | closes when the Task is killed |
| command process group | the watchdog's fork; `{:command, group}` is held with the hands before the command may run | hands (the handle) and watchdog (the life) | at delivery the hands call `Bash.release(handles, :deliver, deadline)`: KILL, and wait until the group is gone | the closed port ends the watchdog's stdin: TERM, 500 ms grace, KILL, reap | the cancel of the hands calls `Bash.release(handles, :cancel, deadline)`: TERM, grace, KILL, and the hands answer only when the release has returned |
| Claude Code program (#10) | `Helyx.Watchdog.start/4` in the harness stream Task, a Task of the hands; `{:watchdog, os_pid}` and `{:command, group}` are held with the hands as for a bash command | hands (the handles) and watchdog (the life) | the stream ends after the program exits or after the 5,000 ms exit wait; at delivery the hands call `ClaudeCode.release(handles, :deliver, deadline)`, the release of the rows "command process group" and "watchdog process", and the terminal goes to the session after it | the closed port ends the watchdog's stdin: TERM, 500 ms grace, KILL, reap | the cancel of the hands kills the stream Task and calls `ClaudeCode.release(handles, :cancel, deadline)`; the abort returns after it, so the next turn starts only when the group is gone. A lost session's first run exits before the fresh run starts; both runs' handles are released together. A later write to a dead watchdog is open, ticket #167 |
| Codex program (#11) | `Helyx.Watchdog.start/4` with open input and the 5,000 ms grace in the harness stream Task, a Task of the hands; the handles are held as for Claude Code | hands (the handles) and watchdog (the life) | after `turn/completed`, Helyx ends the input and the program exits by itself within the 5,000 ms exit wait; at delivery the hands call `Codex.release(handles, :deliver, deadline)`, and the terminal goes to the session after it | the closed port ends the watchdog's stdin: TERM, 5,000 ms grace, KILL, reap | the cancel of the hands stops the stream Task with a 2,000 ms grace, in which it sends `turn/interrupt` and waits up to 1,000 ms, then calls `Codex.release(handles, :cancel, deadline)`, which TERMs the codex group and waits up to 5,000 ms before the KILL; the abort returns after it. A delivery gets the same TERM and wait, because the stream can end while codex still runs a command (a line over the cap, the exit wait). Codex runs each command in a process group of its own, and its `processId` is not an OS pid, so Helyx cannot hold those groups; TERM to the codex group makes codex end them in about 0.5 s (verified), and a KILL leaves them running. So in the normal case no command outlives the release. The watchdog's grace ends when its direct child exits; that child is the Node wrapper, which forwards the TERM and exits only after the native binary (research note, section Processes). Accepted holes: a codex that does not exit within the 5,000 ms grace is KILLed and can leave a command group running, and so can a codex that exits without ending its commands. A lost thread starts the fresh thread on the same run. A later write to a dead watchdog is open, ticket #167 |
| start report pipe (#70) | the watchdog, before the fork | the watchdog (read end) and the held child (write end) | the `exec` closes the write end; the watchdog reads the pipe one time when the child ended by itself, and the exit of the watchdog closes the read end | the watchdog and the child exit on the paths of the rows "command process group" and "watchdog process", and the exits close both ends without a read | the same |
| watchdog process (its own group) | `Port.open` in `Helyx.Watchdog.start/4`, called by the bash tool; `{:watchdog, os_pid}` is held with the hands | the port | exits after it reaps the command | the closed port is its signal; it ignores TERM, and the bash release KILLs it only after every command group is gone or still held, so no KILL cuts the reap short | the release waits for it, so an abort cannot return while the command is unreaped |
| session file parse process (#64) | `Session.File.resume/3` | the caller, monitored, not linked: a link would carry the heap kill to the caller | exits with the parsed result as its exit reason | not released: it finishes its bounded work alone (one read of at most the file limit, a heap of at most 1 GiB, measured under 3.5 s) and exits; the caller has no abort | none |

A call can hold several handles; the hands hold them per Task, and every one is released. A handle that no release confirms is remembered as unconfirmed (see the bounds table). A child that leaves its process group on purpose, with `setsid`, `set -m`, or a `setpgrp` of its own, stays a documented limit: no release sees it, and the release confirms the handles of the call.

## Transcript and events

- Message shape and session file format: see ADR 0001 and the section below.
- Session files live under `~/.helyx/sessions/<project>/<session>.jsonl`.
- Twelve events: `agent_start`, `agent_end`, `turn_start`, `turn_end`, `message_start`, `message_update`, `message_end`, `tool_execution_start`, `tool_execution_end`, `queue_update`, `model_change`, `harness_session`. Each carries the session id, the turn id (nil on the queue drain between turns and on every `model_change`), and a sequence number.

### Session file

One JSON object per line. Every entry has `id`, `parent_id`, `ts`, and `type`. The first entry is the header and its `parent_id` is null. Example entries, one per kind:

```json
{"id":"01J...","parent_id":null,"ts":"2026-09-17T10:00:00Z","type":"session","version":1,"cwd":"/path/to/repo","model":"opencode-go/kimi-k2"}
{"id":"01J...","parent_id":"01J...","ts":"...","type":"message","role":"user","content":[{"type":"text","text":"List the tests."}]}
{"id":"01J...","parent_id":"01J...","ts":"...","type":"message","role":"assistant","model":"opencode-go/kimi-k2","stop_reason":"tool_use","usage":{"input":120,"output":40},"content":[{"type":"text","text":"Listing."},{"type":"tool_call","id":"call_1","name":"bash","arguments":{"command":"ls test"}}]}
{"id":"01J...","parent_id":"01J...","ts":"...","type":"message","role":"tool_result","tool_call_id":"call_1","tool_name":"bash","is_error":false,"content":[{"type":"text","text":"core_test.exs"}]}
{"id":"01J...","parent_id":"01J...","ts":"...","type":"model_change","model":"claude-code/opus"}
{"id":"01J...","parent_id":"01J...","ts":"...","type":"harness_session","provider":"claude-code","harness_session_id":"..."}
```

Content blocks: `text` has `text`. `thinking` has `thinking` and an optional `signature`. `tool_call` has `id`, `name`, and `arguments`. `image` has `mime_type` and base64 `data`.

Rules:

- The header carries `version`. A reader rejects a version it does not know.
- `Helyx.Message.stop_reasons/0` owns the closed `stop_reason` set, and the format encodes it: `end_turn`, `tool_use`, `max_tokens`. A reader decodes them without touching the atom table's history, so a fresh VM resumes a saved file. A terminal event with a stop reason outside the set, or a usage the file cannot encode, fails the turn as a malformed stream event before the message exists. A new stop reason is a format change.
- A tool result links to its call by `tool_call_id`. A tool call with no result after the turn ended gets an `aborted` error result on resume (see Abort).
- Streamed partial messages are not written. Only the completed message is appended, after the provider finishes it.
- A file is read line by line. A last line that does not parse is a torn write. On open, the file is truncated to the end of the last line that parses before anything is appended.
- On resume, the leaf is the last entry in file order.
- A reader never raises at the caller. A header with an unknown or missing version, and an entry with a shape the writer never produces, come back as `{:error, reason}`. An optional value is the exception: it is not needed to resume, so a bad one does not lose the chat. On a message, a `usage` that is not a map reads as `%{}`, and a `model` that is not a string or a `stop_reason` outside the set of `Helyx.Message.stop_reasons/0` reads as nil, as a missing one does. The role, the content, `tool_call_id`, `tool_name`, and `is_error` keep their checks. A bad harness session label is dropped (see the harness session row). Only the last line can be repaired: a bad line mid-file is a malformed file and is rejected without truncation. A repair that cannot write, and a sessions directory that cannot be written on create, are their own error classes.
- Provider deltas that are not valid UTF-8, and tool calls whose fields the file cannot encode, fail the turn as malformed stream events; prompts that are not valid UTF-8 are rejected at the client. Transcript text is valid, and every value in it survives the round trip to the file, from the moment it exists.
- Tool output is repaired to valid UTF-8 at its two boundaries, before it enters the transcript (row "tool result text validity"). So every consumer of the transcript, the file, the providers, and the TUI, sees valid text.
- A write failure mid-session logs a warning, turns persistence off for that session, and the turn continues on the in-memory transcript.

Bounds:

| What | Bound | Over the bound |
|---|---|---|
| Error line of `mix helyx` | One line. Text from the file goes through `inspect/1` with its default limits, or is an exception message; the `inspect/1` limits count characters, so the longest line measured is about 16.5 KB, from a version string of 4-byte characters in a header of at most 65,536 bytes. The clean pass is linear in the text | No hard cap on the length. Control characters and bytes that are not UTF-8 become `?` |
| Session file on resume | One regular file of at most 67,108,864 bytes (64 MiB), read whole into memory; the read stops one byte past the limit, so a file that grows during the resume is still over it. The limit is on file bytes. The lines are parsed one at a time, never split into a list of all lines. The read and the JSON decode run in their own process under `:max_heap_size` of 1,073,741,824 bytes (1 GiB), killed when the heap passes it (#64). The cap bounds every kind of decode growth: the review of #58 measured 12 to 42 bytes of heap per file byte for many short entries, for arrays of short values, and for deep nesting in one line; each of these files is now rejected in under 3.5 s. The file binary is off-heap and bounded by the file limit. The transcript, at most the cap, is copied to the caller, then through the session supervisor into the session process. The supervisor does not keep the arguments of a temporary child, but the start message stays in its heap as garbage, and an idle supervisor does not collect it: measured at 226 MB in the supervisor for a 123 MB session heap (48 MB file). The session supervisor starts with `hibernate_after: 0`, so it hibernates after each message. The hibernation is a full garbage collection, so the supervisor keeps no copy after a start or a resume. This is true when the start fails and when the caller dies during the start (#103). The cost is one full collection for each message to the supervisor. This cost grows with the number of sessions: with 20,000 children, a start takes about 2 ms, and less than 0.01 ms without the hibernation. Local mode runs few sessions, so this is accepted. A session of 66.9 MB of text-heavy messages resumes in 0.5 s; a session of only one-word messages passes the cap near 50 MiB (48 MiB of 153-byte messages resumes, 63 MiB does not). Decoded strings can keep the file binary alive for the life of the session | `{:error, {:too_large, text}}`; the text names the file limit or the heap cap and says to start a new session; the file is not mutated. A last entry that lacks its newline counts with it, so a repair never makes a file that the next resume rejects. Compaction bounds the transcript itself (#1) |
| Header scan for the most recent session | The first 65,536 bytes of at most 256 regular `*.jsonl` files in the project directory. The scan takes the files with the newest modification time. The path breaks a tie. Each regular file uses one of the 256 places, also a file that is not a session and a session of a different directory with the same slug. A header line that does not end inside the 65,536 bytes is not read whole. The directory listing and one `stat` per listed file have no bound on the count, one file per session started there; they read no file content. The triage of #62 accepted this for checkpoint one; measured at 948 ms for 20,000 files. Each scanned file gets a second `stat` before its open, and the resumed file gets a third before the full read | A longer header line is cut, does not decode, and the file is not a session; a session older than the 256 newest files is not scanned, and the result is `:not_found` when no scanned file has the working directory; a pipe, a device, or a directory is skipped without an open; the window between the type check and the open is open, ticket #65 |
| Project directory slug | Last 100 characters of the slugged cwd | Collisions are disambiguated by the header `cwd` |
| Prompt text | Must be valid UTF-8 | `{:error, :invalid_utf8}` at the client boundary |
| Steer and follow-up queues | 32 entries each; entry text is human input, size accepted as unbounded (#29) | The session returns `{:error, :queue_full}` |
| `cwd` at session start (`Session.start/2`, `Session.resume/2`) | A binary, valid UTF-8, with no NUL byte. The session checks it before it reads or creates a file or starts a process. It goes into the session file as JSON, into the system prompt, and to `Port.open` of a harness, which cuts it at a NUL byte. `Session.File.create` does not check it again, nor the model, which the model ref row checks | `{:error, :invalid_cwd}`; the session does not start and nothing is written |
| Tool specs at session start (`Session.start/2`, `Session.resume/2`, through `Helyx.Tool.specs/1`) | `name` a non-empty string of valid UTF-8, unique among the tools; `description` a string of valid UTF-8; `parameters` a map with string keys at the top level that `Helyx.Message.encodable?/1` accepts. No size limit: the plugins are compiled into the node. The callbacks `name/0`, `description/0`, and `parameters/0` run once per session, in the caller, before the sessions directory is read and before any file or process is created. Plugin code that raises, throws, or exits while a spec is built or checked (a callback, or the JSON encoder of a struct in `parameters`) is a bad spec with the module name as its label, as `Helyx.Provider.turn/1` contains a failed `turn/0`, so a plugin failure never reaches the caller of `start/2` or `resume/2`. The first bad tool in registration order is named (#142). An exit signal that plugin code sends to the caller, such as `Process.exit(self(), reason)` or the crash of a process that the plugin linked to it, is not an exit that a `catch` stops, and it still ends the caller, as for `turn/0`: accepted, because a plugin is compiled into the node. The session stores the checked specs; every provider call and the hands use that stored value and never call the callbacks again. JSON Schema validation of `parameters` is out of scope (#142) | `{:error, {:bad_tool_spec, label}}`, where the label is the name, or the module name when the name is bad; two tools with one name give `{:error, {:duplicate_tool_name, name}}`. The session does not start and nothing is written |
| Tool checks at session start (`Session.start/2`, `Session.resume/2`, through `Helyx.Tool.check_available/1`) | The optional `check/0` of each tool runs once per start or resume, in the caller, right after `Helyx.Tool.specs/1` and before the sessions directory is read, the file is created or repaired, or any process starts (#150). The hands do not run it. It runs before the model ref resolves, so a start with a bad model and a failed check gives the check error. The tools run in the order of `specs/1`, sorted by name, and the first failure stops the check. `:ok` passes. `{:error, reason}` passes its reason when the reason is a string of valid UTF-8. The reason has no size limit, as for a spec: the plugins are compiled into the node, and `CodingAgent.error_text/1` prints it on one line. Any other value gives the fixed reason `check/0 returned a bad value`. A `check/0` that raises, throws, or exits gives the fixed reason `check/0 raised, threw, or exited`, as for a spec callback (#142). The check has no deadline: a `check/0` that does not return blocks the caller, as a spec callback does. Accepted, because a plugin is compiled into the node. An exit signal sent to the caller is not contained, as for a spec callback | `{:error, {:tool_unavailable, name, reason}}`, where `name` is the checked spec name. The session does not start, no new file is written, and a resumed file does not change |
| Model ref (`--model`, `Session.set_model/2`, the `/model` argument, the model of a resumed file) | Valid UTF-8, at most 256 bytes, `provider/model` with both parts present, no whitespace and no Unicode category C character (control, format, unassigned), so a printed ref carries no terminal control sequence. Characters that show as nothing (U+3164, U+2800, a variation selector) are accepted, so two refs that look the same can differ; the effect is cosmetic | `{:error, {:invalid_model_ref, ref}}`; the error holds the caller's own string, and a TUI notice shows at most the provider id, never the whole ref. The notice of `bad_provider_id` shows a module name instead; the Provider id row states its bound |
| Provider id (`id/0` of each provider plugin, through `Helyx.Provider.find/2`) | A binary (`is_binary/1`). An id that is not valid UTF-8 matches no ref, because a ref is valid UTF-8. `find/2` calls `id/0` of every provider plugin in the caller of a start, a resume, or a switch, and contains a raise, a throw, or an exit (#153). An exit signal sent to the caller is not contained, and `id/0` and `turn/0` have no deadline: a callback that does not return blocks the caller. For `/model` the caller is the TUI process, so the TUI then answers no key. Both are accepted, because a plugin is compiled into the node: the exit signal as for `turn/0`, and the missing deadline as for a tool `check/0`. The checks cover only a raise, a throw, an exit, and the return value. Every other effect of `id/0` and `turn/0` is accepted for the same reason, also when the resolve succeeds: a change to the process state of the caller (the mailbox, a process flag such as `trap_exit`, the process dictionary, a link, a monitor), and a change to other processes or the node, also one that makes a later step of the same call exit, such as the `GenServer.call` of `set_model/2`. There is no size limit, for the same reason. On a resume the ref comes from the file, so the file can be repaired before this error, as for every model ref error (accepted, #104) | `{:error, {:bad_provider_id, module}}` for any valid ref, because `find/2` must know every id to know that a match is unique. The TUI notice and `CodingAgent.error_text/1` show the module through `inspect/1`. A module name has at most 255 characters, and `inspect/1` writes each in at most 8 bytes (`\x{FFFE}`), so the name is at most 2,043 bytes. The TUI render path drops C0 and C1 controls and wraps the notice at the terminal width; `error_text/1` makes each category C character a `?` |
| Event text into the composer (a key code, a paste) | Must be valid UTF-8. The input widget raises `ArgumentError` on other text, and a raise in a callback stops the TUI process. One private function in `Helyx.TUI`, `edit/3`, is the only path from event text to the widget, and it checks that the text is a binary and that `String.valid?/1` is true before the call. Exception: `textarea_set_value/2` gets only the literal `""`. The terminal backend gives a Rust `String`, so no known source sends such text today; the guard protects the process if a source appears. Size is not checked: the composer is unbounded human input (#29) | The TUI rejects the full text and does not repair it: a replacement would send text the user did not type. The composer does not change, and the status bar shows the reason `input rejected: not valid UTF-8` until the next key press or paste (#46). The reason is one field, so many rejected events use no more memory than one |
| `/model` command line in the composer | The composer text is a command line when it starts with `/model` after any run of Unicode whitespace and category C characters (a pasted BOM, a zero width space). A command line is never sent as a message. The word must end the line or be followed by at least one such character; the rest of the composer text, trimmed, is the ref. A paste keeps tabs and new lines (`docs/features/multiline-composer.md`), so a pasted tab or a new line is a separator. Whitespace after the ref, new lines included, is trimmed, and text on a later line makes an invalid model ref. Text that has `/model` only on a later line is a message. The rule reads the composer text with its paste markers expanded. The TUI reads only the head of the line and does not split or measure the rest: the composer is unbounded human input (#29), and the model ref row bounds the argument, size first | No ref, or no separator after the word (`/models`, `/modelfake/x`), shows `usage: /model provider/model`; a ref with whitespace or a category C character inside, or over the size, is an invalid model ref; the text stays in the composer. A prompt cannot start with `/model`. The rule does not cover looks: a line with an invisible character inside the word (`/mo` U+200B `del`), an invisible character outside category C before it (U+3164, U+034F, a variation selector), or a homoglyph is a message and goes to the model as a prompt. No pattern closes that class; accepted, the cost is that of a typo, and no trust boundary is crossed. A category C character after the ref is not trimmed, so such a ref is an invalid model ref |
| TUI cell list and the search for an open tool cell (`Helyx.TUI.ViewModel`) | The cell list has no limit of its own. It grows by one cell for each message, tool call, and notice, so the session and the human bound it. Each `tool_execution_end` event makes one pass over the cells to find the newest open tool cell with the same `tool_call_id` (#83). The cost is linear in the cell count for each tool result, the same order as the append of a cell. The size is accepted as unbounded, like the composer (#29); a limit on the cell list: accepted for checkpoint one, with no ticket. The reason: the render of the transcript is also linear in the cells on each frame, so the search adds no new order of cost | Nothing is cut. A result that matches no open tool cell is ignored and does not raise. A cell that has a result never changes |
| Transcript row width (`wrap/2` in `Helyx.TUI`) | One private function, `wrap/2`, makes the rows of each transcript line from the line and the width. Every transcript line goes through it by `styled_lines/3`. The status line does not wrap and is not in this row. The goal: no row is wider than the transcript width in the columns that ExRatatui draws (#40). A grapheme is never split. The wrap counts columns with a short rule in `Helyx.TUI`, not with the Unicode tables. ExRatatui has no width function in Elixir, and OTP has no `:string.width/1`. The rule adds the columns of the code points of a grapheme, as ExRatatui does for most text. A code point is two columns when it is East Asian Wide or Fullwidth, or an emoji that is wide by default. An emoji that is narrow by default, for example U+2764, is one column with no selector. Five classes are zero columns. The combining marks: U+0300 to U+036F, U+20D0 to U+20F0, and U+FE20 to U+FE2F. The zero-width spaces, joiners, and direction marks: U+200B to U+200F. The variation selectors: U+FE00 to U+FE0F. The emoji tags: U+E0000 to U+E0FFF. The Hangul vowels and finals: U+1160 to U+11FF. U+17A4 is two columns and U+17D8 is three. All other code points are one column. A code point before the emoji selector U+FE0F is two columns or more. The rule can count more columns than ExRatatui draws, and must never count less. It has no rule for an emoji sequence. ExRatatui draws an emoji with a skin tone modifier, a joiner sequence, or a flag as two columns, and the rule counts each emoji in it, for example four columns for a thumb with a skin tone. A mark of a script that is not in the list counts as one column. A test draws lines in the ExRatatui test terminal and checks that no row is cut. The test has five ranges of code points: U+0020 to U+007E, U+00A0 to U+D7FF, U+E000 to U+20FFF, U+3F000 to U+3FFFD, and U+E0000 to U+E0FFF. U+20000 to U+3FFFD is one range of the rule, so its two ends stand for it. The test draws each code point in 13 contexts. Context 1 is the code point alone. Contexts 2 to 10 put it after a start: a letter, a wide glyph, a Devanagari letter, a modifier base, two emoji that are not modifier bases, a flag half, and two joiner starts. Contexts 11 to 13 put it before an end: a skin tone, a selector, and a joiner with an emoji. `sanitize/1` removes the control characters first, so they are not in the test. Exception 1: a width less than 1 counts as 1. Exception 2: a glyph wider than the full width gets a row of its own. Exception 3: the test does not have every sequence of code points, so the rule can count less for a sequence that no test has. The move to a width function of ExRatatui, when it has one, removes the rule: ticket #90 | Nothing is dropped by the wrap. When the rule counts more, the row is shorter than the width and the text continues on the next row. For exceptions 2 and 3, ExRatatui cuts that row at the edge of the transcript: the glyphs past the edge do not show, and no other row moves |
| Transcript scrollback (`scroll/4` and `transcript_widget/3` in `Helyx.TUI`) | The scroll position is client state of the TUI process, not a field of the view model: nil, or one pair of a cell index and a row in that cell. It is the first row on the screen. nil means that the view follows the newest output. There is no line cache, so no cache has a limit (#39). Measured on 2026-09-19 at width 100, with cells of 851 bytes and 11 rows: a wrap of all cells is 4.6 ms for 100 cells, 82 to 90 ms for 1,000 cells, and 1.8 to 2.2 s for 10,000 cells. A frame runs for each session event, so a wrap of all cells on each frame is not acceptable, and a cache of all rows has the same cost when a cell changes. Thus no operation wraps all cells. A frame wraps only the cells that it shows: at most one cell for each screen row, and two times in follow mode, one time to find the first row and one time to show the rows. A scroll key wraps only the cells that it passes: one screen of rows for the move, one more screen of rows for the check of the end, and one more for the first PgUp, which finds the first row of the follow view. The check after an event, a client notice, or a resize wraps one screen of rows. One time after a resize to a larger width, or after a new cell takes the index of the open message, the check also wraps the cells that the old row number passes: 4.9 ms for 3,450 cells, then 31 µs for the next event. Each count is of full cells, so the cell at each end adds its rows. Measured with 10,000 cells: a scrolled frame is 0.3 ms, a PgDn is 0.9 ms, and the first PgUp is 4 ms. Each operation also makes passes over the cell list without a wrap, linear in the cell count, as the frame did before. The size of one cell has no limit here: one cell is wrapped whole, as before #39, and the cell list row has the accepted limit. The move is one screen: the terminal height minus the composer rows (at most 10, and down to 3 on a small terminal, `docs/features/multiline-composer.md`) and 1 status row, and 1 row when that is less. The size comes from the `:terminal_size_fn` option, which is the option of `ExRatatui.Server` with the same name, and from `ExRatatui.terminal_size/0` when there is none. Local mode is the only transport. Entry points: the `page_up` and `page_down` key codes of ExRatatui with no modifier, press and repeat; `end` with `ctrl`; and a steer or a follow-up that the session accepts. The invariant: the position is nil, or its row is a row of its cell and the rows from it to the end are more than one screen. Thus a scrolled screen is always full, and the status bar says `scrolled` only then. One function, `settle/1`, holds it after each change that no scroll key makes: each session event (`handle_info/2`), each client notice (`/model`), and each `ExRatatui.Event.Resize`. The tests fold events through `handle_info/2` of `Helyx.TUI`, not through the view model, because the position is client state. The scroll keys hold it with the same check, `hold/4`. Exception 1: the position is an index and a row, not an identity. A new terminal width, or a tool result that arrives when the open tool cell is the first cell on the screen, moves the text under the position by the rows that the cell gains or loses. The open assistant message is the last item, and it is not a cell. A new cell that arrives while the message is open takes the index of the message. If the first row on the screen is in the open message, the view then moves by the rows of that cell. Today only a client notice (`/model`) makes such a cell: the session closes the open message before each other cell. A position with an identity: accepted with no ticket for checkpoint one. Exception 2: one function, `on_screen/2`, reads the terminal size for the scroll keys and for `settle/1`. When the terminal gives no size, the position becomes nil, and the view follows the newest output. A frame that ExRatatui draws at its default size of 80 by 24 then shows the newest output. The check runs at the entry points, not on each frame. A change of the terminal size reaches the TUI as an `ExRatatui.Event.Resize`, and a key that does not scroll and does not change the composer line count does no check. An edit that changes the composer line count runs `settle/1`. Thus a frame between the change and the event can use the old position. Such a frame holds the cost limit: `rows_from/3` skips rows only in the first cell, and a row number past that cell shows its last row. The frame wraps that cell and the cells that fill the screen, not the cells that the old row number passes. That frame can show fewer rows than the screen has. A scroll key in that window does the one check that a resize does, with the same one-time cost, and it moves the view from the corrected position: a PgUp can then show rows below the old ones. Exception 3: at a terminal height of 4 rows or less the transcript has no row on the screen, and the scroll keys still move the position by 1 row | PgUp at the first row stays at the first row, so the position is never above the real rows. A position from which the rows to the end fit the screen becomes nil: the view follows the newest output again, and the position is never below the real rows. A transcript that fits the screen does not scroll. A rejected send does not change the position. A `/model` line goes through `settle/1`. When the terminal gives no size, a scroll key makes the position nil, and the view follows the newest output. The idle key help ends with ` · PgUp scroll`. While the view is scrolled, the status bar shows ` scrolled · PgUp/PgDn · Ctrl+End newest` in place of the key help |
| Model change entries per session file | One per accepted switch, human-paced, each at most about 650 bytes (a 256-byte ref of `"` or `\` characters doubles in JSON, plus `id`, `parent_id`, and `ts`); the count is bounded only by the 64 MiB file limit on resume | See the session file row |
| Harness session entries per session file | One per fresh harness session: each harness turn that starts one (the first of a provider, a lost session, a switch from another provider). `Session.File` writes the two strings it is given and, on resume, restores the last id of each provider. One rule, `Helyx.Message.harness_id?/1`, holds at both entry points: valid UTF-8 of 1 to 256 bytes. On resume, the label is optional: with none, the provider starts a fresh harness session. From a provider, the stream check in `Helyx.Session.Stream` applies it, with a `cut` of 0 or more and within the digit limit of `Helyx.Message.cap_integers/1`, else the turn fails with `{:bad_stream_event, event}`; Claude Code's ids are UUIDs | On resume, an entry whose `provider` is a string and whose `harness_session_id` is missing or fails `Helyx.Message.harness_id?/1` removes the label of that provider, so an earlier, stale label does not come back. An entry whose `provider` is missing or not a string is the larger failure: it removes every earlier label of every provider, because the reader cannot know which label it replaced; each provider then starts a fresh harness session. A later valid entry sets a label again. The chat is kept in both cases. An entry whose `id` is missing or not a string is a malformed file, like a bad model change: the `id` is the identity of the entry. As for a model change, `parent_id`, `ts`, and extra fields are not checked, and an empty `id` or `provider` passes; the count is bounded only by the 64 MiB file limit on resume |
| Harness stdout line (`@line_max_bytes` in `Helyx.HarnessIO`, for Claude Code and Codex) | 16 MiB (16,777,216 bytes) before its newline, whether the newline is in the same port chunk or a later one; only the new chunk is searched for a newline, and memory holds at most the buffer under the cap plus one port chunk. A line holds a whole tool result or hook output (for Codex, a whole `item/completed` with the aggregated output of a command), so its size is set by the tool, not by the protocol | One `{:error, {:line_over_limit, 16777216}}` event ends the stream; the port closes and the watchdog kills the program |
| Harness replay (`@replay_max_bytes` in `Helyx.HarnessIO`, for Claude Code and Codex) | The newest history lines (Claude Code) or items (Codex) within 400,000 bytes of JSON; the replay starts at a line or message with no tool result. The prompt, the user messages at the end of the transcript, is not in this cap; it is human input, accepted as unbounded like a queue entry (#29). For Claude Code, the input, replay and prompt, is copied once into a binary, then held by the port and by the watchdog until the program reads it (row "watchdog input on stdin"). For Codex, each item is encoded once and the `thread/inject_items` line is joined from the kept items, so the line is the cap plus its small frame; the prompt goes in the `turn/start` line | Older messages are left out, counted in `cut` of the `harness_session` event, and the TUI shows a notice |
| Harness error text (`@error_max_bytes` in `Helyx.HarnessIO`) | Claude Code: the `errors` of a `result` line, joined, or its `result` text, is cut at 2,000 bytes before it goes into the terminal error, and so is its `subtype` (a `subtype` that is not a string is empty). Codex: the `status` and the error `message` of a `turn/completed` that did not complete, and the error `message` of a response to a Helyx request, get the same cut (a value that is not a string is empty, or `unexpected response`). A character cut in half is dropped, and so is every invalid byte (perl's warnings quote the environment as raw bytes). The watchdog's text when the program did not start gets the same cut. The TUI render is cut again, see the row "Error notice and tool call line in the TUI" | The rest of the text is dropped |
| Error notice and tool call line in the TUI (`@render_max_bytes` in `Helyx.TUI.ViewModel`) | The `inspect(error, binaries: :as_strings)` text of a failed turn's error, from any provider (so a control or invalid byte is escaped and the error shows as text, not as a list of bytes), is cut at 8,192 bytes after the `error: ` prefix, and so is the line of a tool call, its name and its arguments, before the wrap (the name and each key are cut before they join the line, the walk over the arguments with `:maps.next/1` stops once the line is over the cut, and the line is cut before and after its newlines become "␤"). The fold makes the line once, when the call starts, and keeps it in the tool cell, so a frame does not pay for the size of the call; the fold pays for `inspect/1` of each value it reaches, once per call, linear in the call and so bounded by the stdout line cap. A character cut in half is dropped. `inspect/1` alone does not bound them: it escapes a character to up to four times its bytes (a control or invalid byte renders as `\xFF`, U+00A0 as `\u00A0`), its limits apply to each string and collection, not to the whole term, and the argument keys are shown raw, so a malformed harness event of nested maps rendered in 1.5 MB and a call of 500,000 keys in 111,118 rows | The rest of the text is dropped |
| Harness exit wait (`@exit_wait_ms` in `Helyx.HarnessIO`) | 5,000 ms from the terminal while the port is open, one deadline, which output cannot extend: each read loop with a deadline checks it before its `receive` (`HarnessIO.overdue?/1`), because a `receive` with a matching message never reaches its `after`. The deadline runs from the `result` line (Claude Code) or from `turn/completed` or an error response, after which Helyx ends the program's input (Codex), so that the program ends by itself and writes its own session, or from the watchdog's report that the program did not start. Output after the terminal is not read: it neither extends the wait nor counts against the stdout line cap. Before the result, the wait is the program's own loop, with no Helyx bound; the abort of the user is the bound, as for the model gateway. This includes a program that exits with no `result` line while a background child keeps stdout open: the stream waits for the child, or for an abort. A Codex program that exits with no terminal ends the stream with `{:codex_exit, status}` | At the deadline the stream ends with its terminal, and the port closes, so the watchdog kills the program; after a lost-session result, the fresh run starts |
| Harness tool calls and messages per harness turn (Claude Code and Codex) | Each assistant line, or Codex notification, is within the stdout line cap (16 MiB). The number of lines, and so of messages and tool calls in one harness turn, is set by the program's own loop; Helyx does not bound it, as it does not bound the provider calls of a model turn. Each message is appended to the transcript and the file, and the TUI renders it like a model turn's message, with the call line cut (row "Error notice and tool call line in the TUI"). The text and thinking of a message come in delta lines, each within the line cap, and the TUI does not cut them, as it does not cut a model turn's text (row "Transcript scrollback"). A tool call id is taken as unique, as the API makes it: when one harness turn has two open calls with one id, the session gives a result to the first and the TUI to the newest, so the screen can show two results swapped; the transcript and the file agree, and so does the replay unless `tool_id/1` joins two ids (section Replay). A Codex call id is the item id of the program; two Codex ids that are not valid call ids can join in the replay only through a SHA-256 collision of their digests. Each call's result comes before any later message: a new message or the end of the turn gives the calls still open their `aborted` results, and a result after that is dropped | Unbounded, and accepted (#111): the number is set by the program's own loop, and the user's abort bounds it, as for the provider calls of a model turn |
| Codex interrupt wait (`@interrupt_wait_ms` in `Helyx.Provider.Codex`, #11) | 1,000 ms from `turn/interrupt`, sent when the stream Task gets the hands' `:shutdown` while a turn runs and no terminal came. The wait reads the program's lines as usual (the line cap holds) and ends at the turn end, the program's exit, or the deadline, which output cannot extend (`HarnessIO.overdue?/1`); then the Task exits. The hands' shutdown grace is 2,000 ms (`@stream_stop_ms` in `Helyx.Session.Hands`), so the wait ends before the kill. The release follows: TERM to the group, 5,000 ms grace, KILL (row "wait for a killed process group") | The Task exits, the port closes, and the release kills the group. Measured with codex-cli 0.155.0: the interrupt answers in under 1 s |
| Codex held events (`held` in `Helyx.Provider.Codex`, #11) | At most 10,000 events (`@held_max`), checked in `order_one/2` before each event is held, the only place that adds to `held`. The held events are the events after a `message_end` that waits for the result of a call of an earlier message: deltas, tool calls, and results, each from one stdout line within the line cap. A result of a waiting call frees them, and any terminal sends them all. The count check and the insert of a held result each cost O(held), so at most 10,000 steps per event (#111). The cap counts events, not bytes: each held event is within the line cap, so the held events can use up to 10,000 × 16 MiB. They are the events that the session appends to the transcript when they go out, and that volume per turn is accepted, bounded by the user's abort (row "Harness tool calls and messages per harness turn"). The TUI shows a held event only when it goes out, so a slow command of an earlier message can delay the live text of a later one; a session that kept calls open across a `message_end` would avoid this, at the cost of a Core change for every provider | The event over the cap, and every later event of its line, is dropped, and one `{:error, {:held_over_limit, 10000}}` event ends the stream. One `item/completed` line can give a call, a `message_end`, and a result, so a result that the program finished can be dropped with its message; the transcript then does not have that call, and the program's own thread keeps it, as in `docs/features/external-turn.md`, section "Known gap". Before it, `settle/1` sends the events of the chunk and then the held events, in order, as at every terminal; no later line is read. The port closes and the watchdog kills the program. An abort or a steer ends the run and drops the held events with the stream (`docs/features/external-turn.md`, section "Known gap") |
| Harness tool result text, as the provider sends it | 65,536 bytes (`@max_tool_result_bytes` in `Helyx.Session.Stream`), checked on the text as sent, before the UTF-8 repair; the stdout line cap bounds it before that. Every output of the provider cut fits: at most 51,201 bytes of lines and a notice of less than 200 bytes. Core counts bytes, not lines: a provider that breaks the contract can send up to 65,537 lines within the limit (65,536 newlines), and the TUI wraps that one cell whole (row "Transcript scrollback"). The TUI shows the text after the UTF-8 repair, so that cell is at most three times the limit, 196,608 bytes (row "tool result text validity") | the turn fails with `{:tool_result_too_large, actual_bytes, 65_536}` through the usual failure path, and every open call gets its aborted result; the error does not hold the text |
| Harness tool result text, after the provider cut | 2000 lines or 51,200 bytes of line content, on whole lines, by `Helyx.Text.truncate/2`, `:tail`, in `Helyx.Provider.ClaudeCode` and `Helyx.Provider.Codex` before they send the event | See the row "tool result text" |
| SSE line from the model gateway | 1 MiB, terminated or not; the buffer overshoots by at most one transport chunk. `Helyx.Provider.OpenAI` decodes each `data:` payload with `JSON.decode/1` in the provider Task, so this cap bounds the decode; there is no heap cap, accepted (#106) | One `{:error, {:line_over_limit, limit}}` event ends the stream |
| Tool call bytes per response (argument fragments with a flat charge each, ids, names, entry keys, a flat charge per call) | 10 MiB across all calls, overshooting by at most one line; integer call indexes at most 10,000. The arguments of each call are decoded with `JSON.decode/1` at the end of the message, so this cap bounds the decode; there is no heap cap, accepted (#106) | One `{:error, {:tool_call_bytes_over_limit, limit}}` event ends the stream; no partial call is emitted |
| Tool call arguments of the OpenAI provider (#146) | A JSON object, or no bytes for `%{}` | A call with an id becomes `{:rejected_tool_call, call, "the arguments are not a valid JSON object"}` with `%{}` as arguments. The session records it and gives it the error result `tool call not run: the arguments are not a valid JSON object`; the text and the other calls of the message stay, and the model can correct the call. A call with no id cannot get a paired result, so it fails the turn with `{:bad_tool_arguments, name}`. No event and no error holds the raw JSON |
| Integer in tool call arguments and in the usage of a provider call | 100 digits, for each integer at any depth of the maps and lists of the arguments, map keys included, because JSON writes an integer key as its digits. The walk covers every compound term. A tuple is walked, because the error reason of a malformed stream event holds the value and `inspect/1` makes digit text too. A struct that holds such an integer becomes the marker string as a whole, because JSON encodes some structs, such as `Date`. Only a provider that builds its own terms can send a tuple, a struct, or an integer key. The `usage` map of a `:done` event gets the same check, because it gets the same encodes; there the marker string replaces the integer and the turn goes on. A 64-bit value has at most 20 digits, so no tool needs more. The JSON encode of an integer is quadratic in its digits: 185 ms for 100,000 digits, 2,991 ms for 400,000, 18,654 ms for 1,000,000 (OTP 28). Before #79 each such call paid that cost one time in the stream check, one time in the session process for the session file, and one time in each later provider request. With the limit, the worst arguments that the 10 MiB limit on tool call bytes permits (103,819 integers of 100 digits) encode in about 50 ms. The check compares each integer with `10 ** 100` and makes no digit text, so its time is linear in the size of the arguments. It is one function, `Helyx.Message.cap_integers/1`. `Helyx.Session.Stream` applies it at the stream boundary. It runs in the provider Task, not in the session process. It applies the function to the arguments and to the usage before the first JSON encode. Every terminal that leaves the Task goes through it, so an error reason from a provider and the malformed event in a `{:bad_stream_event, _}` reason hold no such integer. `Helyx.Session.Stream` builds a new tool call struct from the id, the name, and the capped arguments, and a new `:done` map from the stop reason and the capped usage, so an extra key is dropped and the shape holds after the cap. The reason of a Task exit gets the function where the `{:task_exit, reason}` terminal is made: in the session process for a local turn, and in the hands for the stream of an external turn. On resume, the session file applies the function to the arguments of a tool call block and to the usage of a message, so a file from before #79 gives no such integer to the transcript. Not covered: a provider that raises or exits with such an integer in the reason. The crash report of the Task makes the digit text (about 3 s for 400,000 digits) before the session gets the exit; the events hold the capped reason. Providers are compiled into the node. Accepted with no ticket. The Task tells the session which call it rejected, by value. A second call of the same assistant message with the same id and other arguments runs. A second call that is equal to the rejected call after the replacement is also not run. The JSON decode in the provider runs before the check and is not quadratic: 3 ms for 100,000 digits, 197 ms for 1,262,000. An integer of more than about 1,262,611 digits (4,194,304 bits) is over the integer size limit of the BEAM: `JSON.decode/1` raises `SystemLimitError`, the provider Task exits, and the turn fails in 5 ms with no tool result. The session accepts the next prompt. A clean error for that case is a change in the provider, accepted with no ticket | Arguments or a usage that are a struct, not a plain map, are a malformed stream event, which fails the turn. The function replaces the integer with the string `integer of more than 100 digits removed` in the arguments. The transcript, the events, the session file, and the next provider request hold only this string. The tool does not run. The call gets an error tool result, `tool call not run: an integer in the arguments has more than 100 digits`, with the usual `tool_execution_start` and `tool_execution_end` events, in call order. The other calls of the message run. A float is not changed: its encode is short |
| HTTP error response body | 16 KiB; the accumulator overshoots by at most one transport chunk | The body is cut to valid UTF-8 at the limit and marked `[truncated at the N-byte limit]`; the rest of the response is cancelled |
| Wait for bytes from the model gateway (`@receive_timeout` in `plugins/bundled/lib/helyx/provider/openai.ex`) | 120,000 ms between two chunks of the response, the first chunk included. Req applies it to each chunk, so it is not a limit on the full response. The total time of a response is unbounded, and accepted with no ticket, because the abort of the user is the bound: a gateway that sends one chunk in each 120 s holds the turn until the abort | Before the response starts, the stream gives one `{:error, reason}` event with the transport error. During the body, the timeout raises in the turn Task, and the turn fails as `{:task_exit, reason}`. The session accepts the next prompt |

The three provider limits carry no research citation: `docs/research/coding-tools.md` covers tool output truncation, not gateway input, so the values are set here.

Ownership:

| Resource | Holder | Release |
|---|---|---|
| Session file handle | The calling process; every read and append opens and closes inside one `Session.File` call | On return; no handle outlives a call |

A `Session.start` that fails after the file is created (a supervisor or hands failure) can leave a header-only session that a later resume restores as an empty transcript with the right model. Open, accepted for checkpoint one; the window is narrowed by creating the file only after the plugins resolve.

Two more holes are open and accepted for checkpoint one. Nothing locks a session file: two runtimes that resume the same file both append from the same leaf and interleave their entries. Local mode is one user in one node; a lock lands with a multi-node transport. And a resumed session starts a new event stream: sequence numbers restart at one, and no client API reads the restored transcript. The TUI starts empty after a resume, and the model keeps the full context. Ticket #163 adds the session history to the TUI (#105).

## TUI

- ex_ratatui, alternate screen. The TUI is `Helyx.TUI` in `plugins/bundled`, defined only when `ex_ratatui` is loaded (ADR 0005). It implements no Core interface and is not in Core's plugin list: it is a client that subscribes to one session. Local delivery is OTP messages from `Helyx.Session.subscribe/1`; a Transport interface arrives with the first remote client.
- The view model is a pure fold over events, `Helyx.TUI.ViewModel`, tested with scripted event lists. It grows with the conversation, bounded by the session. A tool result attaches to the newest open tool cell with the same `tool_call_id`, wherever the cell is (#83). A notice can arrive while the tool runs. The session runs tool calls one at a time. The fold does not depend on that order. Core makes every event from checked data, so the fold trusts the shapes of `Helyx.Event`: an event of another shape is a bug in Core and crashes the TUI. The fold ignores `:turn_start`, `:turn_end`, and the `:message_start` of a user message on purpose. The composer is human input; its size is accepted as unbounded, like a queue entry's text (#29). Tool results render at most four content lines each, plus one truncation row naming the hidden line count. Transcript lines wrap by terminal display width, not by grapheme count; the bounds table has the rule and its exceptions (#40).
- PgUp and PgDn scroll the transcript by one screen (#39). While the view is scrolled, new output does not move it, and the status bar says that the view is scrolled. Ctrl+End, a PgDn at the end, or a sent prompt returns the view to the newest output. The position is client state of the TUI process. It is not in the view model, because it depends on the terminal size, and the fold knows no terminal. The bounds table has the costs and the exceptions.
- Escape aborts. Enter sends a steer during a turn and a prompt when idle. Alt plus Enter queues a follow-up: most terminals cannot tell Shift+Enter from Enter without the kitty keyboard protocol, so Alt is the modifier. Ctrl+J adds a new line; the composer and the paste rules are in `docs/features/multiline-composer.md` (#44). The status bar shows the model, the run state, and the queue counts. When the client rejects input, the status bar also shows the reason in red, as ` ✕ <reason> ` at the start of the line: `not sent: the queue is full` for a steer or a follow-up, and `input rejected: not valid UTF-8` for event text. The rejected text stays in the composer. The reason is the `reason` field of the view model. It is client-local, like a notice: `ViewModel.reject/2` sets it, and no event changes it. Each reason is a fixed text of `Helyx.TUI`, so it holds no input and its longest text is 31 bytes. The reason comes first because the line does not wrap and a model ref can be 256 bytes; the reason is at most 35 columns with its mark, so it shows in full at 35 columns or more. The next key press or paste clears the reason before the TUI handles that input, so the same input can set a new reason. A new reject replaces the reason, also when it comes from a key repeat, because the newest reject is the one the user must see. The release and the repeat of a key do not clear it, because those of the rejected Enter come immediately after the reject. There is no terminal bell. A rejected `/model` switch stays a notice cell, because its text must stay visible while the user corrects the ref.
- `/model provider/model` in the composer switches the model through `Session.set_model/2`, idle or during a turn. It is the only command. The rule is on bytes, not on looks: a line that starts with `/model`, after whitespace and category C characters, is never sent as a message, and any other text is a message; see the bounds table for the exact rule and its limit. The status bar changes when the `model_change` event arrives, not when the call returns, so the view model stays a fold over events. A rejected switch adds a notice cell (`ViewModel.notice/2`, client-local like the composer) and keeps the text in the composer.
- Queued steers are delivered together at the next provider call. On an external turn, a steer aborts and resends (see Harness turns).
- The `helyx` Mix task lives in `apps/coding_agent`. `mix helyx [directory] [--model provider/model] [--resume]` starts Core with the bundled plugins, one session in `directory` (default: the current one), and the TUI, and quitting restores the terminal. The session is written under `~/.helyx/sessions`; `--resume` continues the most recent session for the directory with its saved model, so it does not combine with `--model`. The scan for that session reads only the 256 files with the newest modification time in the project directory, and an append sets that time. A session older than those files is not found, and the task prints the `:not_found` sentence, `no saved session for this directory; start one without --resume`, although the file exists. A start or a resume that fails prints one line on stderr, `could not start the agent: <sentence>`, and the task exits with a non-zero status. `CodingAgent.error_text/1` is the one place that makes the sentence. It has a clause for each shape of `Helyx.Session.File.error/0`, for the model ref and provider errors, for `{:tool_unavailable, name, reason}`, and for `{:terminal_init_failed, text}`. The `:too_large` text prints as it is. An invalid model ref stays out of the sentence, as in the TUI notice. A value from the file (`:unknown_version`, the type of a bad entry, a provider id) prints through `inspect/1` with its default limits, so a JSON object shows as a map. A bare atom prints as its POSIX text when it is a POSIX code. An error with no clause prints through `inspect/1`, so it can show as a tuple: a duplicate tool name, a bad tool spec (`{:bad_tool_spec, label}`, #142), a supervisor error, a session that died. Every sentence gets one clean pass: a whitespace run becomes one space, and a control character or a byte that is not UTF-8 becomes `?`. The `:invalid_file` text can be an exception message of many lines, and the clean pass puts it on one line. The task reads the sessions directory from the `:sessions_dir` application env key of `:coding_agent`, so that a test uses its own directory. The default stays `~/.helyx/sessions`.

## Out of scope

MCP, sub-agents, permission prompts, plan mode, todos, background shell, compaction, branching, editing queued messages, queue persistence, remote hands, packaging, Anthropic API key provider, dedicated grep, find, and ls tools, skills discovery. During an external turn, Helyx tools are not visible to the model (ADR 0002).
