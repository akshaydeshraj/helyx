# Review: #121, the tool text helpers move out of Core

Invariant: every harness tool result that joins the transcript is at most 65,536 bytes as the provider sent it, measured before the UTF-8 repair; a larger one fails the turn with `{:tool_result_too_large, actual_bytes, 65_536}` and no text, and the bundled harness providers cut each result with `Helyx.Text.truncate/2`, `:tail`, before they send it.

Entry points: `harness_event/1` in `Helyx.Session.Stream` (the one place where a harness tool result enters the session), the `translate/2` clause of `Helyx.Provider.ClaudeCode` for a `user` line, and `tool_result/1` of `Helyx.Provider.Codex`.

## Round 1 (full)

- Simplify: four agents (reuse, simplification, efficiency, altitude). 0 findings. The simplification agent noted a missing trailing newline in `text.ex`; `mix format` added it.
- Bounds sensor: `bounds sensor skipped: TYPESAFE_API_KEY is not set`.
- Standards: 0 hard violations. 2 items to fix, 5 judgement calls.
  - `docs/features/session-stream.md` still named `Helyx.Tool.truncate/2` in its harness row. Fixed: the row points to the two new rows of `docs/features/coding-agent.md`.
  - `git diff origin/master` showed `docs/features/external-turn.md` as deleted. Not a change of this ticket: `origin/master` got that file after the branch point. The commit does not touch it, and the branch is rebased onto `origin/master` after the commit.
  - Judgement calls, skipped: the name `Helyx.Text` (the feature doc chose it); the limit `65_536` in the code, the moduledoc, and the docs (the moduledoc states the contract); three return shapes of `harness_event/1` (round 2 removed them); full `@doc` in a `@moduledoc false` module (the functions moved unchanged); `String.to_integer/1` in the test provider (test only).
- Spec: 0 wrong implementations, 0 scope creep. 3 gaps.
  - The stale row of `session-stream.md`: the same as above.
  - The base drift: the same as above.
  - Core checks bytes, not lines. Before #121 the session cut enforced 2000 lines for every harness provider. A provider that breaks the contract can now send up to 65,536 lines within the byte limit. The design moves the line cut to the providers, so this follows from it. Fixed in the docs: the row "Harness tool result text, as the provider sends it" of `coding-agent.md` states it. Round 2 corrected the numbers of that row.
- Failure path: 0 findings. A 500-run property probe of `Helyx.Text.truncate(_, :tail)` gave at most 51,263 bytes, under the limit. Both emitters cut. The new error branch uses the halt of the provider error terminal. Accepted: 65,536 invalid bytes pass the check and become 196,608 bytes after the repair, as the feature doc states; the test `result_raw` covers it.

The first `mix precommit` failed: Credo strict found `consume/4` too complex (10, max 9), because its `case harness_event(event)` had three branches. Fix: `harness_event/1` returns only `{:ok, event}` or `{:error, reason}`. A malformed event gives `malformed(event)`, `{:error, {:bad_stream_event, event}}`, with the event as the provider sent it, as before. The comment of `@max_tool_result_bytes` was also made clearer.

## Round 2 (full)

The fix is in one code file, about 20 lines. It adds a function (`malformed/1`) and changes the return shape of `harness_event/1`, so the round is full. The invariant under test: a malformed harness event fails the turn with `{:bad_stream_event, event}`, and the event is the event that the provider sent. Only a well-formed tool result over 65,536 bytes fails with the too-large error.

- Simplify: one agent covered the four angles of this fix. 0 findings applied. Skipped: the `case` in `consume/4` could route the error through the halt of the provider error terminal; the gain is cosmetic. The comment of the limit was reworded.
- Bounds sensor: `bounds sensor skipped: TYPESAFE_API_KEY is not set`.
- Standards: 0 hard violations. Judgement calls:
  - This record was stale. Fixed.
  - Three other places build `{:bad_stream_event, _}` inline. Skipped: this is outside #121, and #122 changes the same file.
  - The literal `65_536` is in the docs and in the moduledoc. Skipped: the attribute is the one source in the code.
  - `too_large/1` returns a reason, and `malformed/1` returns a tuple. Skipped: this is naming only.
- Spec: 0 code findings. 3 doc findings, all fixed:
  - This record was stale.
  - The row of `coding-agent.md` said that the TUI cell stays within the byte bound. The TUI shows the text after the UTF-8 repair, so the cell is at most 196,608 bytes. The row now says this.
  - The same row said 65,536 lines. 65,536 newlines are 65,537 lines under the line rule. The row now says this.
- Failure path: 0 findings. Nine malformed and oversized events went through a session. Each error held the event as sent, except for integers over 100 digits. The cap of #79 at the Task exit replaces those integers, as before this change.

The round 2 fixes changed Markdown only, so no further round.

## Feature doc correction

`docs/features/tool-text-out-of-core.md` said that `test/helyx/interfaces/tool_test.exs` keeps the tests of `by_name/1`, `spec/1`, and `hold/1`. The file held only the text tests, so the whole file moved. Those three functions are tested through `test/helyx/hands_test.exs` and `test/helyx/session_test.exs`.

## Orchestrator

- Codex adversarial review, round 1: approve, 0 findings.
- Accepted: Core enforces only the byte bound for a harness provider that breaks the contract; the 2000-line bound is part of the provider contract. The whole `tool_test.exs` moved, because it held only text tests.
