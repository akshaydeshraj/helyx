# Review: one Mix project for the bundled plugins (ticket #38)

Scope: the working-tree diff for issue #38 against `origin/master` at `d04607b`. Nine plugin projects moved with `git mv` into `plugins/bundled` (app `:helyx_plugins`), `apps/coding_agent` dep list, ADR 0005, `AGENTS.md`, `README.md`, the feature doc, `.credo.exs`. `/ship` review record; every round ran as sub-agents.

Invariant of the change: every bundled module keeps its name, its behaviour, and its tests, and a product gets `Helyx.TUI` exactly when `ex_ratatui` is in its own deps.

## Simplify (4 agents)

| Angle | Finding | Resolution |
|---|---|---|
| reuse | none | |
| simplification | `before-time.log`, a timing file, was staged | Fixed: removed; the numbers are below |
| efficiency | none | |
| altitude | The ADR and the code comment said that `__mix_recompile__?/0` fixes the add case. The agent measured a toy project: the always-present module fixes add, the hook fixes remove | Fixed: both texts state both parts |

## Round 1, full (standards, spec, failure-path)

Bounds sensor, base `origin/master`:

```text
bounds sensor skipped: TYPESAFE_API_KEY is not set
```

| # | Axis | Finding | Resolution |
|---|---|---|---|
| 1.1 | standards | No devlog for the session | Fixed: `docs/devlogs/2026-09-18-consolidate-plugins.md` |
| 1.2 | standards | ADR 0005 prose: two idioms, an unexplained metaphor, one sentence with three results | Fixed |
| 1.3 | standards | `AGENTS.md` Module naming bullet held three instructions in one sentence | Fixed: split |
| 1.4 | standards, spec | Env key `:req_options` now sits in an app that all bundled plugins share; a second HTTP plugin would collide | Fixed: `:openai_req_options` of `:helyx_plugins`, in code, test, ADR, and `AGENTS.md` |
| 1.5 | standards | `Helyx.TUI.Available.available?/0` repeats a word | Accepted: minor, the name says what the `if` reads |
| 1.6 | standards | The guard needs the same four steps for each heavy plugin | Accepted: one case exists; extract on the second |
| 1.7 | standards | `AGENTS.md` hard-coded three projects; "run one plugin's tests" named one file | Fixed |
| 1.8 | spec | Ticket says "the TUI modules" get the guard; only `Helyx.TUI` has it | Accepted, recorded in ADR 0005 and the devlog: `Helyx.TUI.ViewModel` needs nothing from `ex_ratatui`, and a guard on it would need a second always-present module |
| 1.9 | spec | `Helyx.TUI.Available` and its test are not in the ticket | Accepted: without it the acceptance box "With `ex_ratatui` added, `Helyx.TUI` is available" fails for a product that was built before. Measurements in ADR 0005 |
| 1.10 | failure-path | ADR named the wrong error for an absent guarded module. Reproduced: `Helyx.Core.start_link(plugins: [Helyx.TUI, ...])` returns `{:error, {:not_a_plugin, Helyx.TUI}}` and does not raise | Fixed in the ADR |
| 1.11 | spec, failure-path | `before-time.log` untracked at the root | Fixed: deleted |

The failure-path agent built no state table: the diff adds no OS, filesystem, or network operation, and the four tool files are 100% renames. It ran add, remove, and add again of `ex_ratatui` on one `_build`; each gave the correct build.

## Round 2, reduced (spec, failure-path)

The fix of 1.4, without tests and Markdown: 1 code file, 1 line added and 1 removed, no function, module, process, or dependency added or removed, no arity or spec change. So a reduced round. Brief invariant: in the shared app `:helyx_plugins`, each application env key names its plugin, code, tests, and living docs agree on it, and no reader or writer of an old key remains.

Bounds sensor, base: the tree before the fix:

```text
bounds sensor skipped: TYPESAFE_API_KEY is not set
```

| Axis | Finding |
|---|---|
| spec | none. One reader (`openai.ex:66`), two writers (the test), no `config/` directory in any project. Nit: the env key sentence in `AGENTS.md` is not in the ticket; kept, it states the invariant |
| failure-path | none. 42 tests pass; no second path found |

Deviations from the skill: none.

## Scratch product (acceptance box 4)

A product under `.scratch/product` with `helyx` and `helyx_plugins` as path deps:

- Without `ex_ratatui`: `mix compile --warnings-as-errors` exit 0, no warning, no `ex_ratatui` in its lock. `Helyx.Core.start_link/1` with `Provider.Fake`, `Provider.OpenAI.Go`, `ModelContext.Default`, `Compaction.None`, and the four tools returned `{:ok, pid}`. `Helyx.TUI` not loaded.
- With `ex_ratatui` added to that built product: exit 0, no warning, `Helyx.TUI` loaded, `Helyx.TUI.run/1` exported. Removed again: `Helyx.TUI` gone. Added again: present.

## Root `mix precommit`, before and after

Each run is cold: a fresh worktree before, all `_build` and `deps` directories deleted after. The machine was suspended or busy during both after runs (3% CPU over the wall time), so the after wall times say nothing. CPU time is the number to read.

| Run | Projects | user | sys | CPU (user+sys) | Wall |
|---|---|---|---|---|---|
| before, `origin/master` `d04607b` | 11 | 229.89 s | 27.01 s | 256.90 s | 12:59.92 |
| after, run 1 | 3 | 209.59 s | 20.55 s | 230.14 s | 1:37:07.77 (machine suspended) |
| after, run 2 | 3 | 256.95 s | 24.71 s | 281.66 s | 2:13:51.61 (machine suspended) |

The two after runs differ by 51 s on the same tree, which is more than the distance of either from the before run. So these numbers show no CPU gain that is larger than the noise. A cold run is mostly dependency compiles and the PLTs that include `ex_ratatui`; there were two of those before (`plugins/tui`, the app) and there are two after (`plugins/bundled`, the app). The gain of the ticket is three lock files and three projects, not cold CPU time. A valid wall time needs a run on a machine that stays awake.

## Precommit

Passed, both after runs. Root: 1 property, 96 tests, 0 failures. `plugins/bundled`: 125 tests, 0 failures (124 moved, 1 new). `apps/coding_agent`: 5 tests, 0 failures.

Log lines in `plugins/bundled` that are not failures: two `GenServer ... terminating ** (stop) killed` from `Helyx.Hands` in the TUI tests, which the before run also printed in `plugins/tui`; and in run 2 only, one `Task ... exited in: GenServer.call(... Helyx.Provider.Fake ...) no process`, a provider Task of a session whose test had ended and stopped its Core. All test files are 100% renames except the OpenAI env key and the one new TUI test.
